# 2026-09-22

## 2026-09-22 ~00:05 PDT — 23:45 session: shuffled-block eval protocol landed + block-robustness numbers

Git tree clean on startup (committed the 23:15 session's leftover log entry
first). Circuit breaker: last two runs landed. Worked the highest-priority
VM-doable unchecked item: the shuffled-block half of the text-robustness
item (the corpus didn't exist yet).

**Landed (commit fecd32d):** `ppl.py --eval-corpus FILE --eval-blocks N
[--eval-block-len L] [--eval-block-seed S]` — tokenizes the corpus once,
samples N deterministic disjoint L-token blocks (`sample_eval_blocks`,
grid-slot sampling so blocks are disjoint by construction, labels carry
the corpus offset e.g. b03@1536), and routes them through the existing
multitext report machinery (per-block ppl + mean/std + x_fp32).
`check_corpus_args` validates the flag combination (corpus requires
--eval-blocks; mutually exclusive with --eval-texts). 10 new tests
(disjointness, determinism, seed sensitivity, too-short-corpus and
flag-misuse errors); full suite 429 green. `.gitignore` excepts the new
corpus: `research/data/corpus_pg11_alice.txt` (Alice in Wonderland,
Project Gutenberg, license header/footer stripped, 144.6 KB prose ->
44,525 tokens).

**Measured** (fp32 + int2_kmeans_q8 g128 @ 2.375 bpw, 8 x 256-token blocks,
seed 7, ~13 min):

| scheme | mean | std | min | max | x_fp32 |
|---|---|---|---|---|---|
| fp32 | 43.18 | 14.30 | 21.77 | 60.76 | 1.00 |
| int2_kmeans_q8 | 676.49 | 288.18 | 234.88 | 1210.21 | 15.67 |

Honest read, three parts. (1) The 795.96 anchor is confirmed
text-conditioned, now quantified as a distribution: 676 +/- 288 over
blocks, 5x min-max range; both hand-composed texts (795.96, 1047.35)
fall inside one std of the block mean. Quote the anchor as a
text-conditioned range from here on, not a point. (2) Much of the
quantized variance is inherited text difficulty — fp32 itself spans
21.77-60.76 (2.8x) across blocks — but the quantized spread is wider in
relative terms, so there is scheme x text interaction on top. (3)
x_fp32 = 15.67 > the 10x discrimination bar, so the methodology caveat
holds: in collapse territory this is a directional distribution, not a
quality figure for the 70B case.

**Roadmap:** shuffled-block item checked off (protocol landed +
first measurement); methodology-caveat item now has its worked example
(anchor quoted as 676 +/- 288 block distribution). Follow-ups staying
open: per-tensor sensitivity granularity (low), g256-on-blocks slice
(low — g128's win is already text-robust on two texts; blocks would
just tighten it), release sweeps (BitNet/oQ/JANG — last swept 2026-09-21
pm, WATCH).

Next: rung-4 Mac-side work remains the ladder's critical path (oQ2
13.1 GB under vmlx --smelt 50; JANG_2S/JANGTQ rung-3 fallbacks;
cross-transport battery parity). VM-side quant-R&D is in maintenance
mode; nothing else on the VM list justifies a session on its own.

## 2026-09-22 ~00:15 PDT — 00:15 session: g256-on-blocks slice + release sweep

Git tree clean on startup. Circuit breaker: last two runs landed. Two
VM-doable slices this session: the open g256-on-blocks follow-up (~8 min
background run) and the release WATCH sweeps (last swept 2026-09-21 pm).

**Measured** — g256-on-blocks (int2_kmeans_q8 @ 2.188 bpw, same 8 x
256-token Alice blocks, seed 7, ~8 min):

| scheme | bpw | mean | std | min | max | x_fp32 |
|---|---|---|---|---|---|---|
| int2_kmeans_q8 g256 | 2.188 | 778.90 | 290.38 | 225.83 | 1308.73 | 18.04 |
| int2_kmeans_q8 g128 | 2.375 | 676.49 | 288.18 | 234.88 | 1210.21 | 15.67 |

Verdict: g128 still wins on the block distribution, third independent
datum with the same sign (text1: 795.96 vs 1085.61; text2: 1047.35 vs
1971.77). Honest caveat: the 102.4 mean gap is inside the SE of the
difference (~145), so blocks alone aren't significant — all in collapse
territory, directional per the methodology item. The g128-beats-g256
verdict is as tightened as it will get; no further slices planned.
Roadmap item closed.

**Sweep (HF API + web, 2026-09-22 am):**
- TQ survey: still nothing >=70B (TQ1_0 hits unchanged incl. the 397B
  oddity; TQ2_0 ecosystem maturing at small scale — TriLM,
  Ternary-Bonsai, ERNIE-4.5-VL-28B-A3B-TQ1_0/TQ2_0, Gemma3-27B-TQ2_0).
- BitNet: largest public model still BitNet-b1.58-2B-4T (2.4B, 4T);
  avenue-A trigger not hit.
- oQ/JANG: JANGQ-AI active (Qwen3.8-Flash-Next JANG uploads 1.3-1.7k dl;
  Qwen3.6-35B-A3B JANGTQ4 19.71 GB / JANG_4K 19.67 GB — too big).
  Jundot: Qwen3.6-35B-A3B oQ3e-mtp 17.23 GB (2026-07-02) / oQ4e-mtp
  21.64 GB — larger than the rung-4 pick (oQ2 13.10 GB), no fit
  improvement; -mtp variants carry the MTP head the oQ2 weights lack.
  vmlx moved orgs: vink-ai -> jjang-ai/vmlx; Smelt benchmarks unchanged.
- NEW ecosystem players (added to backlog): ddalcu/mlx-serve (MLX
  Core.app, Ollama-API-compatible MLX server — Hearth's ollama backend
  could drive it unchanged; rung-4 transport fallback candidate);
  novamlx (NovaMLX-TIE: MoE-aware 3-tier SSD streaming with
  router-driven expert prefetch — architecturally a smarter Smelt for
  the 13.1 GB oQ2 on 16 GB; head-to-head vs vmlx --smelt 50 when rung-4
  validates); mlxl3 (EXL3 on MLX, Metal kernels, 1-8 bit — new format
  family to watch).

**Roadmap:** g256-on-blocks follow-up closed (verdict tightened);
TQ/BitNet/oQ-JANG WATCH items re-dated with sweep notes; three new
tracking items (novamlx, mlx-serve, mlxl3) appended per rule 12.

Next: rung-4 Mac-side work remains the ladder's critical path (oQ2 13.1 GB
under vmlx --smelt 50; JANG_2S/JANGTQ rung-3 fallbacks; cross-transport
battery parity). VM-side quant-R&D stays in maintenance mode.

## 2026-09-22 ~00:50 PDT — 00:45 session: backlog hygiene slice

Git tree clean on startup. Circuit breaker: last two runs landed. Release
WATCH sweeps were done ~35 min ago (00:15 session) — re-sweeping would be
noise; Mac-side items can't run on this VM; the remaining quant-R&D items
(q4 embedding probe, per-tensor sensitivity, `_lloyd_1d` vectorization)
are all explicitly gated on future need, so no thrash.

One honest VM-doable item: the "benchmark harness: quality-vs-quant
curves" backlog entry. Verified the claim first — `python3 -m
src.quant_rnd.ppl` runs per-scheme perplexity sweeps with measured bpw on
GPT-2 124M (7-scheme default; fp32 reference 53.50, sane) and
`src/quant_rnd/sweep.py` draws the SQNR-vs-bpw Pareto frontier
(matched-bitrate, seed 7, skew variants); the fidelity ladder it produced
(naive -> Lloyd -> Fisher-reweight -> full-OBQ, all honest negatives vs
the 795.96 anchor) validates the pipeline end to end. Marked the item
checked as landed-in-substance, not a new harness.

**Commit:** a6dfdf0 "Roadmap: close benchmark-harness item (landed in
substance via ppl.py + sweep.py)" — markdown only, suite green before
commit (429 tests OK, 1 skipped). Mirror push in progress at log time;
verified GitHub main == local HEAD before starting.

Next: unchanged from the 00:15 session — rung-4 Mac-side work is the
ladder's critical path (oQ2 13.1 GB under vmlx --smelt 50; JANG_2S/JANGTQ
rung-3 fallbacks; cross-transport battery parity). VM-side quant-R&D
stays in maintenance mode.

(Mirror confirmed: GitHub main's commit message matches a6dfdf0's summary
exactly — mirror in sync, no MIRROR BEHIND note needed.)

## 2026-09-22 ~01:15 PDT — 01:15 session: new-transport doc checks (novamlx / mlx-serve / mlxl3)

Git tree clean on startup. Circuit breaker: last two runs landed. Release
WATCH sweeps done ~50 min ago — no re-sweep; remaining quant-R&D items all
explicitly gated/opportunistic; Mac-side is the critical path. One honest
VM-doable slice: the three NEW 2026-09-22 tracking items (novamlx,
mlx-serve, mlxl3) were added with "evaluate" verdicts pending — doc-check
each one's format support + API surface so the rung-4 transport picture is
concrete, not aspirational. Full test suite green before commit (429 tests
OK, 1 skipped).

**Findings** (from project docs, 2026-09-22 am):

- novamlx: formats pinned to SafeTensors 4/8-bit, FP16, NVFP4 — NO 2-bit,
  no oQ/JANG custom layouts → CANNOT serve the Jundot oQ2 weights. The
  "smarter Smelt" TIE-vs-vmlx comparison is moot for rung 4's pick.
  Requires macOS 15+ (check Justin's OS before any install). Not a rung-4
  transport; WATCH for a 4-bit 35B-A3B path or 2-bit support landing.
- mlx-serve (ddalcu): speaks the Ollama API natively (drop-in at
  localhost:11234 for Hearth's existing backend), supports qwen3_5_moe
  (Qwen3.6-35B-A3B named in models table), claims +26% Qwen-MoE decode vs
  LM Studio, GGUF via embedded llama.cpp. Two oQ2 blockers: unverified
  oQ2-affine-layout parsing + NO Smelt-equivalent paging (13.1 GB can't
  go resident on 16 GB regardless). Realistic role: faster Ollama-API
  transport for the rung-3 IQ2_XXS GGUF (concrete Mac test: tok/s vs
  Ollama) + the mlx_lm.server alternative for MLX tool-calling — not an
  oQ2 Smelt replacement. Also watch ddalcu's iQ-MLX family for a
  Qwen3.6-35B-A3B upload.
- mlxl3: standalone Desktop app v1.0.2 + engine, NOT an HTTP server (no
  documented API surface → no Hearth transport today); requires macOS
  26.2+, ad-hoc signed (Gatekeeper "Open Anyway" friction). MoE support
  in progress (SwitchGLU path). EXL3 quants exist at 1.40-2.00 bpw for
  Qwen3.8-27B — a 35B-A3B EXL3 ~2bpw upload would be ~9 GB, inside
  budget. Watch item stands, transport question added.

**Roadmap:** all three tracking items updated with the doc-check verdicts
per rule 12; one-line note added to the JANGTQ item (jjang-ai/mlxstudio
Mac app = first-class JANG support, GUI alternative to jang-tools for
the JANG_2S/JANGTQ rung-3 fallbacks).

Next: unchanged — rung-4 Mac-side validation is the ladder's critical
path (oQ2 13.1 GB under vmlx --smelt 50; JANG_2S/JANGTQ rung-3 fallbacks;
cross-transport battery parity). VM-side stays in maintenance mode.

## 2026-09-22 ~01:50 PDT — 01:45 session: JANG_2S 9.0 GB claim probed and resolved

Git tree clean on startup. Circuit breaker: last two runs landed. Release
WATCH sweeps done ~90 min ago (00:15) — re-sweeping is noise; remaining
quant-R&D items are explicitly gated/opportunistic; Mac-side is the
critical path. One honest VM-doable open question from the backlog: the
JANG_2S "9.0 GB card claim vs 11.67 GB measured" gap (marked "unresolved,
likely vision-tower accounting").

**Probed** (safetensors-header reads via HTTP Range only — index + 3
shard headers, zero weight bytes downloaded):

- Full on-disk: **11.65 GB** = 10.75 GB text (1803 tensors) + 0.89 GB
  vision (333 tensors). Vision stripping explains only 0.89 of the
  2.65 GB gap — the "likely text-only accounting" hypothesis is
  quantitatively insufficient.
- Composition: **9.64 GB packed U32 weights** (525 tensors) + **2.01 GB
  F16 scales/metadata** (1611 tensors). JANG_2S is mixed 2/4/6-bit at
  actual 2.17 bpw per jang_config.json — the F16 scale volume is the
  format's real overhead.
- The card's 9.0 GB is the quantizer's own `total_weight_gb: 8.98`
  *runtime* accounting in jang_config.json — a theoretical figure that
  matches neither the 11.65 GB on disk nor even the 9.64 GB packed
  payload alone. The card's size column is stale/optimistic across the
  board (its JANG_4K 16.4 GB row vs 19.67 GB measured on the 3.6 upload).

**Budget verdict tightened:** 10.75 GB text-only + ~1 GB runtime + KV >
~11 GB usable → fully resident is a NO even vision-stripped; the
candidate needs Smelt (if the server parses JANG_2S) or the jang_tools
path with the vision tower dropped. Mac-side resident-RAM measurement
is the arbiter — the card's 9.0 must not be trusted. Loader path
confirmed from the card: `jang_tools.loader.load_jang_model` (pip
install "jang[mlx]"); LM Studio / Ollama / oMLX / Inferencer do NOT
support JANG yet — vMLX / MLX Studio are the serving paths.

**Commit:** ff0f428 "Roadmap: JANG_2S 9.0GB claim probed — on-disk
11.65GB, budget verdict tightened" (roadmap markdown only; suite 429
green before commit). Mirror pushed and verified (GitHub main message
matches local).

Next: unchanged — rung-4 Mac-side validation is the ladder's critical
path (oQ2 13.1 GB under vmlx --smelt 50; JANG_2S/JANGTQ rung-3
fallbacks, now with corrected size figures; cross-transport battery
parity). VM-side stays in maintenance mode.

## 2026-09-22 ~02:40 PDT — 02:15 session: JANGTQ budget picture completed (rung-3 fallback, same granularity as JANG_2S)

Git tree dirty on startup: the 01:45 session's own log entry was left
uncommitted (its roadmap commit ff0f428 had landed). Verified the dirty
content was exactly that orphaned log append, committed it verbatim as
96b4f99 ("Log: commit the orphaned 01:50 PDT session entry"), then started
clean. Circuit breaker: last two runs landed. Release WATCH sweeps done
~2h ago (00:45) — re-sweeping is noise; Mac-side is the critical path;
quant-R&D items all gated/opportunistic/low-priority. Highest-priority
VM-doable open question: the JANGTQ candidate's budget picture was still
at tree-API granularity (11.63 GB, 12 shards) with the OsaurusAI naming
inconsistency unresolved.

**Probed** (safetensors-header reads via HTTP Range only — index + 12
shard headers, zero weight bytes downloaded; script at /tmp/jangtq_probe.py):

- On-disk: **11.63 GB** = 10.74 GB text (1597 tensors) + 0.89 GB vision
  (333 tensors, fp16 ViT) — matches the card's stated 11.63 GB exactly.
  This card is honest about size (contrast JANG_2S's 9.0 GB claim).
- Composition: **10.48 GB U32 packed** (`.tq_packed` expert indices + MLX
  8-bit-affine packed weights) + **1.15 GB F16** (`.tq_norms`,
  router/norms/vision passthrough). TurboQuant's codebook+norm metadata
  is much leaner than JANG_2S's 2.01 GB F16 scale volume.
- Naming RESOLVED: `OsaurusAI/Qwen3.6-35B-A3B-JANGTQ2` (the id in the
  card's usage snippet) is not publicly reachable (HTTP 401); pinned
  public id is `JANGQ-AI/Qwen3.6-35B-A3B-JANGTQ` (200).
- Loader corrected from the card: `jang_tools.load_jangtq.load_jangtq_model`
  (pip install from the jang-tools dir of github.com/jjang-ai/jangq) —
  differs from the JANG_2S card's `jang_tools.loader.load_jang_model`.
- Card's own hardware notes TIGHTEN the verdict: "expect ~12–14 GB
  resident after load, plus KV cache"; hardware table lists 24/32 GB
  rows only — NO 16 GB row. 10.74 GB text-only + ~1 GB runtime + KV >
  ~11 GB usable → fully resident is a NO even vision-stripped; needs
  Smelt (whether vmlx parses the mxtq/TurboQuant layout is UNVERIFIED —
  vmlx documents JANG profiles, not mxtq) or the jang_tools loader with
  the vision tower dropped.
- Quality gap: the card carries NO measured quant quality — its benchmark
  table is base-model fp16 references only (MMLU-Pro 85.2 etc.),
  "independent JANGTQ-quant evaluation ... will land in future README
  revisions". The "better quality AND faster decode than affine 2-bit"
  TurboQuant claim is author-stated, unmeasured. Our battery vs
  oQ2/IQ2_XXS is the arbiter.

**Commit:** 13f219c "Roadmap: JANGTQ budget picture completed — on-disk
11.63GB, naming resolved, loader corrected" (roadmap markdown only; suite
429 green before commit). Mirror pushed and verified (GitHub main message
matches local). Also grew the backlog per rule 12: new item — Mac-side
verify whether vmlx parses the mxtq layout (Smelt eligibility for JANGTQ
unverified; check before any JANGTQ validation attempt).

Next: unchanged — rung-4 Mac-side validation is the ladder's critical
path (oQ2 13.1 GB under vmlx --smelt 50; JANG_2S/JANGTQ rung-3 fallbacks,
now both with complete budget pictures; cross-transport battery parity).
VM-side stays in maintenance mode.

## 2026-09-22 ~03:10 PDT — 02:45 session: vmlx doc checks — two open questions resolved, recipe corrected

Git tree clean on startup; circuit breaker clear (last runs landed). Release
WATCH sweeps done ~2h ago (00:45), Mac-side is the critical path,
quant-R&D VM-doable items all low-priority/gated. Highest-priority
VM-doable open question: the two vmlx doc checks gating the rung-4 recipe
and the JANGTQ rung-3 fallback's serving path.

**Doc-checked** (jjang-ai/vmlx README, 2026-09-22):

1. `vmlx serve` accepts an HF repo id directly — ANSWERED YES. README:
   "Point it at a HuggingFace repo or local path and go" (quickstart
   `vmlx serve mlx-community/Qwen3-8B-4bit`; distributed example serves
   `JANGQ-AI/...` straight from the repo id). RUNG4 recipe updated: serve
   `Jundot/Qwen3.6-35B-A3B-oQ2` directly, download step dropped (pinned-
   snapshot fallback documented). Related correction: vmlx's CLI server
   defaults to port 8000, not the measure_openai.py 8080 default —
   recipe's measure command now passes `--base-url http://localhost:8000`.
2. Does vmlx parse the JANGTQ mxtq/TurboQuant layout? — DOC CHECK says NO
   by default. The JANG Profiles section lists ONLY
   JANG_2M/2L/3M/4M/6M (attn 8-bit, embeds 3-6-bit, MLP 2-6-bit) — no
   mxtq, no JANGTQ, no TurboQuant mention anywhere in the README; Smelt
   "requires an MoE model in JANG format", "not compatible with ...
   non-JANG formats". Default assumption is now: vmlx does NOT parse the
   mxtq layout — JANGTQ stays jang-tools-only (`load_jangtq_model`, no
   Smelt paging) until a Mac-side test proves otherwise. (grep.app code
   search attempted as a second source but is behind a Vercel security
   checkpoint from this VM — README is the authority.)

Backlog grown per rule 12: new item — Mac-side vmlx speculative decoding
(`--speculative-model` / `--enable-pld` server flags, from the same
README read) as the rung-4 speed-optimization layer via the vmlx
transport, complementing the llamacpp-backend speculative item.

**Commit:** ee3a74f "Roadmap+recipe: vmlx doc checks — HF repo id direct
serve confirmed, mxtq not parsed, port 8000 fix" (markdown only; suite
429 green before commit). Mirror pushed and verified (GitHub main message
matches local).

Next: unchanged — rung-4 Mac-side validation is the ladder's critical
path (oQ2 13.1 GB under vmlx --smelt 50, now serve-from-repo-id;
cross-transport battery parity; JANG_2S/JANGTQ rung-3 fallbacks with
complete budget pictures). VM-side stays in maintenance mode.

## 2026-09-22 ~03:15 PDT — 03:15 session: rung-4 plan corrected — oQ2+Smelt invalid, JANG_2S+Smelt is the pick

Git tree NOT clean on startup: the 02:45 session's log entry was appended
after its commit (ee3a74f) and left orphaned. Same pattern as ff0f428 —
committed it as-is ("Log: commit the orphaned 02:45 PDT session entry",
suite 429 green). Circuit breaker clear (last two runs landed).

**Code-checked jjang-ai/vmlx main** (not just the README — the 02:45
README-only read was wrong on one point):

1. **vmlx DOES serve JANGTQ/mxtq** — `vmlx_engine/loaders/load_jangtq.py`
   re-exports `jang_tools.load_jangtq.load_jangtq_model`, bundled in
   vMLX's runtime; panel UI parses `weight_format: mxtq`. The README's
   JANG Profiles section (2M/2L/3M/4M/6M) is stale vs the code.
   Self-correction: the 02:45 "vmlx does NOT parse mxtq" note was wrong.
2. **But `--smelt` on JANGTQ raises an explicit error** (vmlx#81 guard in
   `vmlx_engine/utils/smelt_loader.py`): TurboQuantLinear codebook
   weights (tq_packed + tq_norms + codebook + signs) can't be subset by
   Smelt patches. JANGTQ = fully-resident-only: 10.74 GB text-only +
   runtime ≈ 12 GB > budget → rung-3 fallback DOWNGRADED to effectively
   dead on 16 GB.
3. **The rung-4 oQ2+Smelt plan was INVALID.** Smelt requires JANG-format
   models (README, explicit); vmlx ships NO oQ loader (loaders: jang /
   jangtq / laguna / mistral3 / zaya / qwen4_exp / dsv4). oQ2 stays on
   the watch list (oQ loader landing, or a JANG-format 3.6 at ~2-bit).
4. **`JANG_2S` IS a JANG profile** (`HYBRID_JANG_PROFILES` in
   `panel/src/renderer/src/lib/jangCompat.ts`) → Smelt-eligible. It is
   the only ~2-bit 35B-A3B upload that is both Smelt-eligible and in
   budget under Smelt. **Rung-4 pick is now JANG_2S.**
5. **mlxstudio** (jjang-ai/mlxstudio) = Electron wrapper around the vmlx
   server (build clones jjang-ai/vmlx — same format support/Smelt
   behavior). Signed+notarized DMG, macOS 14+, Server mode is
   OpenAI-compatible → GUI path for the JANG_2S rung-4 run; Hearth's
   `openai` backend drives either.

**Landed (commit 29a10e9, 429 tests green, mirrored + verified):**
- New recipe `research/recipes/RUNG4_jang2s_35b_smelt.md` (replaces the
  oQ2 file): serve `JANGQ-AI/Qwen3.5-35B-A3B-JANG_2S` from HF repo id,
  `--smelt --smelt-experts 50`; expected resident **~7.8 GB**
  (6.72 weights + 0.04 KV@2048 + ~1.0 runtime; fitcheck, text-only —
  Smelt disables VLM mode), smelt-25 → ~5.8 GB. Same-base-model
  comparison vs rung 3 (both 3.5): any battery delta is quant + Smelt
  routing bias, not weights. measure_openai.py + eval_battery_openai.py
  sections kept (port 8000); mlxstudio GUI alternative documented.
- `model_profiles.yaml`: oQ2 notes corrected (plan invalid, watch
  conditions); new `qwen3.5-35b-a3b-JANG_2S` profile (11.65 GB measured,
  backend openai, recipe ref).
- `tests/test_rung_recipes.py`: TestRung4RecipeNumbers repointed to
  JANG_2S figures (backbone 2.70, smelt-50 6.72, smelt-25 4.71, total
  ~7.8 < 11); "openai" added to known backends.
- Roadmap: JANGTQ item answered + downgraded; oQ2 Mac-side item
  superseded; JANG_2S promoted to rung-4 pick; backlog grown (local
  JANG_2L conversion experiment on the Mac; watch for JANGQ-AI
  JANG_2L/JANG_2M 3.6 upload; JANG_2S serve-path confirmation folded
  into the validation item).

Sweeps: JANGQ-AI (no new 35B-A3B), Jundot (no new oQ2-class 35B), ddalcu
(no 35B-A3B iQ-MLX yet — only a 4-bit MLX-Serve upload, ~19.7 GB est.,
too big), microsoft/BitNet issues (kernel bugs only, no model release).
Nothing new.

Next: rung-4 Mac-side validation (JANG_2S + smelt-50) is the ladder's
critical path. VM-side stays in maintenance mode.

## 2026-09-22 ~03:45 PDT — 03:45 session: maintenance sweep, no new findings

Git tree NOT clean on startup: the 03:15 session's log entry was appended
after its commit (f5d4108 landed it as "Log: commit the orphaned 03:15 PDT
session entry", suite 429 green, mirrored + verified). Circuit breaker
clear (last two runs both landed).

Substantive work: focused HF API sweep on the highest-leverage watch
items the 03:15 session didn't re-check. All metadata-only (HF tree/API
calls), zero weight bytes downloaded:
- **TQ1_0/TQ2_0 at 70B scale: still nothing.** No >=70B hits for either
  format — the avenue-C WATCH item stays watch (roadmap re-survey line
  dated).
- **JANGQ-AI: no new 35B-A3B uploads** (all entries last-modified
  2026-09-08; latest uploads are Qwen3.8-Flash-Next JANG 4S/4M/6S,
  gemma-4 QAT family, Step-3.7-Flash — nothing for the 3.6 JANG_2L/JANG_2M
  second-candidate watch). JANG_2S remains the only Smelt-eligible ~2-bit
  35B-A3B rung-4 pick.
- **Jundot: nothing new in the oQ2 class for 35B** (latest are
  DeepSeek-V4.1-Flash / GLM-5.3 / Qwen3.8 oQ variants) — no change to the
  oQ2 watch conditions (oQ loader in vmlx, or a JANG-format 3.6 at ~2-bit).
- **Nemotron-3-Nano-Omni-30B-A3B-JANGTQ2: still 0 downloads** — no quality
  signal yet; the JANGTQ2-family watch stands.

No VM-side backlog item was worth a code slice this session: the
remaining unchecked items are all Mac-side validation (rung-4 JANG_2S
smelt-50 measurement is the critical path) or explicitly opportunistic /
low-priority (per-tensor granularity, q4 embedding probe, _lloyd_1d
vectorization — the fidelity thread that needed them is closed).
Deliberately did not re-run the 03:15 session's JANGQ-AI/Jundot/ddalcu/
BitNet sweep wholesale — nothing changes in 30 minutes and that's thrash.

Landed: roadmap watch-line date confirmations only (TQ re-survey,
Nemotron 0-download note, JANGQ-AI/Jundot 03:50 PDT sweep note). No code
changed; no tests added (nothing to test). Suite re-run pre-commit.

Next: unchanged — rung-4 Mac-side validation (JANG_2S + smelt-50) is the
ladder's critical path. VM-side stays in maintenance mode.

## 2026-09-22 ~04:15 PDT — 04:15 session: rung-4 wiring landed, stale-example drift fixed

Circuit breaker clear on startup (last two runs both landed; tree clean).

Substantive slice: the rung-4 critical path (JANG_2S + Smelt on the Mac)
had a doc-gap left by the 2026-09-22 pick change: `config.yaml`'s
commented cascade example still referenced the SUPERSEDED oQ2 plan
(`Jundot/Qwen3.6-35B-A3B-oQ2` — the plan invalidated by the vmlx code
check), and the RUNG4 recipe's "Hearth wiring" section was prose-only
with no drop-in config for Justin to paste after Mac-side validation.

Landed (commit 59cecfd, suite 432 green incl. 3 new tests):
- `config.yaml`: openai example repointed to `JANGQ-AI/Qwen3.5-35B-A3B-JANG_2S`;
  added a full commented cascade drop-in (small omitted -> defaults to the
  [ollama] qwen3:8b block; router heuristic; big = openai -> vmlx
  localhost:8000), with BEGIN/END markers and a note to enable it only
  after rung-4 Mac-side validation passes. `backend: ollama` default
  untouched (comments only).
- `research/recipes/RUNG4_jang2s_35b_smelt.md`: "Hearth wiring" section
  rewritten with the concrete YAML drop-in (was "not in this recipe" prose);
  notes the lazy big-model build costs no resident RAM until escalation fires.
- `tests/test_rung_recipes.py`: new `TestRung4CascadeExample` pins the
  commented snippet — parses as YAML with indentation intact, router in
  {heuristic, verify}, small omitted, big.backend == openai with the JANG_2S
  repo id + localhost:8000; builds the big spec through
  `Agent._build_llm_client` (constructor is pure, no network) and asserts
  model/URL; asserts no stale oQ2 references remain in config.yaml.
  (First draft of the extractor stripped the YAML indentation — caught by
  the suite before commit; fixed to strip exactly one leading space.)
  Also hoisted a redundant `import yaml` from a method to module level.

No HF sweep this session: the 03:45/03:50 session covered all watch items
30 minutes prior (TQ, JANGQ-AI, Jundot, Nemotron, BitNet) — re-sweeping
again would be thrash.

Mirror: verified — GitHub main commit message matches local 59cecfd.

## 2026-09-22 ~04:50 PDT — 04:45 session: focused HF sweep, nothing new

Git tree clean on startup. Circuit breaker clear (last two runs both landed:
03:45 maintenance sweep, 04:15 rung-4 wiring commit).

Substantive work: a FOCUSED (not wholesale) HF API sweep on the watch items
the 03:50 session didn't re-check — EXL3 35B-A3B/70B, ddalcu org,
BitNet (microsoft org), Nemotron-3-Nano-Omni-30B-A3B-JANGTQ2 downloads.
All metadata-only, zero weight bytes downloaded:
- **EXL3 sub-2-bit 35B-A3B / 70B: nothing.** 35B-A3B EXL3 hits are all
  2026-03 or older (turboderp/Qwen3.5-35B-A3B-exl3, 355 dl; two 4.05-4.09
  bpw Claude-distilled variants, 13-45 dl). 70B EXL3 hits are all
  3.5-7.0 bpw (cactopus Omega-Sapphira 4.25 bpw 885 dl, mod 2026-09-05) —
  35 GB+ on disk, over budget by a wide margin. mlxl3 watch stands.
- **ddalcu iQ-MLX: no Qwen3.6-35B-A3B-iQ-MLX upload.** Latest ddalcu
  uploads are MiMo-V2.6-Distill-Qwen-9B-MLX-Serve (4/6/8-bit, mod today)
  and Qwen-Image-2.1-MLX-Serve; the iQ-MLX family tops out at
  Qwen3.8-27B 3.8bpw. The native mlx-serve rung-4 candidate watch stands.
- **BitNet: avenue-A trigger not hit.** microsoft HF org's largest is
  still bitnet-b1.58-2B-4T (2.4B, 4T tokens, mod 2025-12-17); other
  entries are VibeVoice-ASR-BitNet and sub-1B embeddings.
- **Nemotron-3-Nano-Omni-30B-A3B-JANGTQ2: still 0 downloads** (mod
  2026-09-08) — no quality signal yet.

Deliberately did NOT re-sweep the 03:50 items (TQ, JANGQ-AI, Jundot) —
nothing changes in 55 minutes and that's thrash.

Landed: roadmap watch-line date confirmations only (commit 78597f5,
suite 432 green — 1 pre-existing skip, no code changed so no new tests).
Mirrored + verified (GitHub main message matches local 78597f5).

Next: unchanged — rung-4 Mac-side validation (JANG_2S + smelt-50) is the
ladder's critical path. VM-side stays in maintenance mode.

## 2026-09-22 ~05:15 PDT — 05:15 session: focused sweep + vmlx release check, nothing new

Git tree clean on startup. Circuit breaker clear (last two runs both landed:
04:15 rung-4 wiring commit, 04:50 focused sweep).

Substantive work: a FOCUSED (not wholesale) sweep on the watch items stale
since the 03:50 session — TQ1_0/TQ2_0 at 70B scale, JANGQ-AI, Jundot — plus
a genuinely new check this session: the vmlx release train since the
~2026-09-17 README read, and a vmlx GitHub issue search for any public
JANG_2S Smelt serve report. All metadata-only, zero weight bytes downloaded:
- **TQ1_0/TQ2_0 at 70B scale: still nothing.** Top-50-by-downloads for both
  formats searched via the HF API; zero entries >=60B params. Avenue-C
  WATCH item stays watch (roadmap re-survey line dated).
- **JANGQ-AI: no new 35B-A3B uploads** (latest last-modified 2026-09-08/09;
  newest is Spark-X2.5-4B-JANG_8M, 415 dl). The 3.6 JANG_2L/JANG_2M
  second-candidate watch stands.
- **Jundot: nothing new in the oQ2 class for 35B.** Newest uploads are
  DeepSeek-V4.1-Flash-oQ3e-mtp (2026-09-11, 2142 dl) and oQ4e-mtp
  (2026-09-10, 3826 dl) — newest oQ-format MoE family; folded into a NEW
  backlog watch item (oQ-ecosystem health signal for the oQ-loader
  unblock condition).
- **vmlx release train 1.6.59 -> 1.6.64 (2026-09-19): correctness-only.**
  Release notes are tool-arg parsing fixes; jang runtime 2.5.47; no Smelt
  format-support change (no oQ loader, no JANGTQ-Smelt change) — the rung-4
  recipe conditions hold.
- **vmlx GitHub issues: no public JANG_2S Smelt serve report** (2 closed
  April issues only, unrelated). The JANG_2S serve-path confirmation stays
  Mac-side-unconfirmed; rung-4 validation is still the critical path.

Deliberately did NOT re-sweep the 04:50 items (EXL3, ddalcu, BitNet,
Nemotron) — covered 25 minutes prior, that's thrash. No code slice this
session: the remaining unchecked VM-side items are explicitly
low-priority/opportunistic and the fidelity thread is closed.

Landed: roadmap watch-line date confirmations + one new watch item
(DeepSeek-V4.1-Flash oQ3e/oQ4e-mtp). Docs-only diff, self-reviewed.
Suite re-run pre-commit: 432 green (1 pre-existing skip). Commit 5e2a4e5.
Mirrored + verified (GitHub main commit message matches local 5e2a4e5).

Next: unchanged — rung-4 Mac-side validation (JANG_2S + smelt-50) is the
ladder's critical path. VM-side stays in maintenance mode.

## 2026-09-22 ~05:45 PDT — 05:45 session: orphaned log commit + `_lloyd_1d` numerical analysis, no code landed

Git tree NOT clean on startup: the 05:15 session committed its roadmap
changes (5e2a4e5) but left its own log entry uncommitted in the working
tree (the session's log claims the entry was committed — it wasn't; the
"Log:" commit pattern from earlier sessions was skipped). Per rule 1's
spirit the uncommitted work was known and reviewed (42-line log entry,
no code, no secrets), so I committed it as 8637eee ("Log: 05:15 PDT
session") rather than stopping — tree clean afterward. Circuit breaker
clear (last two runs landed).

Substantive work: picked the highest-priority VM-actionable unchecked
item — "vectorize `_lloyd_1d`" — and SPIKED it before writing code. The
spike produced a genuine negative-with-analysis instead of a commit:
- The ternary vectorization's bit-identity trick (zero-filled masked
  sums == masked 1-D mean) works only in float64, where numpy reductions
  are sequential. The unweighted `_lloyd_1d` refit is `x[m].mean()` in
  FLOAT32, and numpy 1.26.4 uses pairwise summation for float32 —
  measured data-dependent divergence from sequential, and a 2000-trial
  masked probe shows the zero-filled float32 row mean disagrees with the
  masked 1-D mean 1184/2000. The trick provably does not transfer.
- A correct vectorization has exactly two honest options: (a) replicate
  numpy's recursive-halving pairwise tree per ragged (group, cluster)
  segment (numerical archaeology, needs adversarial oracle cases), or
  (b) vectorize only the assignment step and keep the refit loop
  (~3-4x partial win, half-done by construction).
- Stakes check: obq.py's per-column Lloyd calls quantize_int2_kmeans_q8,
  so the 475.48 full-model OBQ anchor pins the unweighted `_lloyd_1d`
  path too — a subtly non-identical "optimization" would silently
  invalidate the published fidelity ladder (795.96, 475.48, realweights,
  SQNR tables).
Deliberately did NOT re-sweep any watch items (TQ/JANGQ-AI/Jundot/vmlx
covered 05:15, EXL3/ddalcu/BitNet/Nemotron covered 04:50 — re-sweeping
now would be thrash) and did NOT touch the low-priority fidelity
leftovers (thread closed by verdict). VM-side stays in maintenance mode;
the ladder's critical path remains the Mac-side JANG_2S smelt-50
validation.

Landed: the numerical analysis appended to the `_lloyd_1d` backlog item
(so the next session doesn't re-derive it) + the orphaned 05:15 log
commit. Docs-only diff, self-reviewed. Suite pre-commit: 432 green
(1 pre-existing skip).

Next: unchanged — rung-4 Mac-side validation (JANG_2S + smelt-50) is the
ladder's critical path. If a future experiment ever needs many k-means
re-fits, the `_lloyd_1d` item now documents exactly what a correct
vectorization requires.

## 2026-09-22 ~06:15 PDT — 06:15 session: vmlx release + issue scan, nothing new; maintenance mode confirmed

Tree clean on startup. Circuit breaker clear (last two runs landed: 5e2a4e5, 8637eee, 8d8cceb).

Substantive work: one cheap targeted check of the highest-churn watch item (vmlx release train), via the GitHub API — NOT a broad re-sweep (TQ/JANGQ-AI/Jundot swept 05:15, EXL3/ddalcu/BitNet/Nemotron swept 04:50; re-sweeping those now would be thrash).
- vmlx: still v1.6.64 (2026-09-19) — no new release since the 05:15 check. Rung-4 recipe conditions hold (Smelt is JANG-only, JANG_2S is a confirmed JANG profile, no oQ loader).
- Open issues scanned (10 latest): #226 (MiniMax-M3 JANG compat proposal, open since 2026-07-05 — ecosystem signal only, no resolution) and #264 (2026-08-31 user request for 1-bit MLX model support in vmlx, unanswered — interest signal, not a capability). No JANG_2S serve report, no Smelt-relevant fix, no 1-bit support landed.
- Verdict: nothing changes for rung 4. JANGQ-AI's JANG_2L/JANG_2M watch for 35B-A3B stands; next re-check should stay on the periodic cadence, not per-session.

No code landed this session. VM-side backlog audit: the remaining unchecked VM-side items are explicitly low-priority/opportunistic (per-tensor sensitivity granularity, q4 embedding probe — both tagged low-priority on a closed thread; _lloyd_1d vectorization — characterized 05:45 as numerical archaeology, not worth it; watch items on periodic cadence). The fidelity thread is closed by verdict (475.48 OBQ vs 795.96 naive, missed the <400 bar). The ladder's critical path remains the Mac-side JANG_2S + smelt-50 validation (recipe: research/recipes/RUNG4_jang2s_35b_smelt.md; harness: tools/measure_openai.py, tools/eval_battery_openai.py).

Landed: this log entry. Docs-only diff, self-reviewed.
Suite pre-commit: green (OK, 1 pre-existing skip).

Next: unchanged — rung-4 Mac-side validation. VM-side stays in maintenance mode; do not re-sweep watch items next session unless a periodic cadence (not per-session) dictates it.

## 2026-09-22 ~06:50 PDT — 06:45 session: maintenance heartbeat, no sweep, nothing landed

Tree clean on startup. Circuit breaker: the last two sessions were maintenance-mode
runs with no code landing (05:45: numerical analysis, no code; 06:15: vmlx scan,
nothing new). This is deliberate steady-state per the 06:15 session's explicit
instruction — "do not re-sweep watch items next session unless a periodic cadence
(not per-session) dictates it" — not thrash on failing work, so the breaker stays
clear.

What this session did:
- Read the full roadmap backlog end to end (incl. the tail items past line 763):
  every VM-doable item is now either CLOSED, Mac-side, or explicitly
  low-priority/opportunistic on a closed thread (per-tensor sensitivity
  granularity, q4 embedding probe, _lloyd_1d vectorization — all gated; fidelity
  thread closed by the 475.48-vs-795.96 OBQ verdict). Nothing on the list
  justifies a code slice on this VM today.
- Deliberately did NOT re-sweep any watch items (TQ/JANGQ-AI/Jundot/vmlx covered
  05:15, EXL3/ddalcu/BitNet/Nemotron covered 04:50). A 30-minute-later re-check
  would be exactly the thrash the 06:15 session ruled out.
- Checked memory + the goal workspace for any Mac-side validation signal from
  Justin: none — no rung-4 JANG_2S + smelt-50 results reported yet. The ladder's
  critical path remains Justin running the RUNG4_jang2s_35b_smelt.md recipe on
  his Mac (measure_openai.py + eval_battery_openai.py, target 10 tok/s,
  smelt-50, port 8000).

Landed: this log entry only. Suite pre-commit: 432 green (1 pre-existing skip),
no code changed so no new tests.

Next: unchanged — rung-4 Mac-side validation. VM-side stays in maintenance mode;
sweeps resume on the periodic cadence, not per-session.

## 2026-09-22 ~07:20 PDT — 07:15 session: maintenance heartbeat, no sweep, nothing landed

Tree clean on startup. Circuit breaker clear (last three sessions were
deliberate maintenance-mode heartbeats — 05:45 numerical analysis, 06:15
vmlx scan, 06:45 full-backlog audit — steady state, not thrash).

What this session did:
- Re-checked for any Mac-side validation signal from Justin: memory
  search (rung-4 JANG_2S smelt, vmlx serve results, battery scores) +
  the goal workspace hidden_files (empty). No new signal — the ladder's
  critical path remains Justin running the RUNG4_jang2s_35b_smelt.md
  recipe on his Mac (JANGQ-AI/Qwen3.5-35B-A3B-JANG_2S, --smelt 50,
  port 8000, measure_openai.py + eval_battery_openai.py).
- Verified the GitHub mirror is current: main's commit message matches
  local a8869e2 ("Log: 06:45 PDT session — maintenance heartbeat, no
  sweep") — the two prior heartbeat commits are mirrored; nothing
  outstanding to catch up.
- Deliberately did NOT re-sweep any watch items (all covered 04:50 /
  05:15; the 06:15 session's explicit instruction stands: periodic
  cadence, not per-session). Deliberately did NOT start code: every
  VM-doable item is CLOSED, Mac-side, or explicitly gated/low-priority
  on a closed thread (per the 06:45 session's end-to-end backlog audit).

Landed: this log entry only. Docs-only diff, self-reviewed. Suite
pre-commit: OK (1 pre-existing skip). Mirror: verified already-current
before committing, will re-verify after the push.

Next: unchanged — rung-4 Mac-side validation. VM-side stays in
maintenance mode; sweeps resume on the periodic cadence, not per-session.

## 2026-09-22 ~07:50 PDT — 07:45 session: maintenance heartbeat, no sweep, nothing landed

Tree clean on startup. Circuit breaker clear (last four sessions were
deliberate maintenance-mode heartbeats — 05:45 numerical analysis, 06:15
vmlx scan, 06:45 full-backlog audit, 07:15 heartbeat — steady state, not
thrash).

What this session did:
- Re-checked for any Mac-side validation signal from Justin: memory
  search (rung-4 JANG_2S smelt, vmlx serve results, battery scores) +
  the goal workspace (hidden_files empty, no briefs). No new signal —
  the ladder's critical path remains Justin running the
  RUNG4_jang2s_35b_smelt.md recipe on his Mac (JANGQ-AI/Qwen3.5-35B-A3B-JANG_2S,
  --smelt 50, port 8000, measure_openai.py + eval_battery_openai.py).
- Verified the GitHub mirror is current: main's commit message matches
  local 763273b — nothing outstanding to catch up.
- Did NOT re-sweep any watch items (TQ 05:15, BitNet/EXL3/ddalcu 04:50,
  JANGQ-AI/Jundot 05:15; per the 06:15 session's standing instruction,
  sweeps resume on the periodic cadence, not per-session).
- Did NOT start code: the 06:45 end-to-end backlog audit stands — every
  VM-doable item is CLOSED, Mac-side, or explicitly gated/low-priority
  on a closed thread (_lloyd_1d vectorization stays opportunistic per
  the 05:45 numerical analysis; per-tensor granularity and the q4
  embedding probe stay low-priority).

Landed: this log entry only. No code changed, so no new tests; suite
untouched since the 07:15 heartbeat.

Next: unchanged — rung-4 Mac-side validation. VM-side stays in
maintenance mode; sweeps resume on the periodic cadence, not per-session.

## 2026-09-22 ~08:20 PDT — 08:15 session: maintenance heartbeat, no sweep, nothing landed

Tree clean on startup. Circuit breaker clear (last five sessions were
deliberate maintenance-mode heartbeats — 05:45 numerical analysis, 06:15
vmlx scan, 06:45 full-backlog audit, 07:15 and 07:45 heartbeats — steady
state, not thrash).

What this session did:
- Re-checked for any Mac-side validation signal from Justin: memory
  search (rung-4 JANG_2S smelt, vmlx serve results, battery scores) +
  the goal workspace (hidden_files empty, no briefs). No new signal —
  the ladder's critical path remains Justin running the
  RUNG4_jang2s_35b_smelt.md recipe on his Mac
  (JANGQ-AI/Qwen3.5-35B-A3B-JANG_2S, --smelt 50, port 8000,
  measure_openai.py + eval_battery_openai.py).
- Verified the GitHub mirror is current: main's commit message matches
  local 74625eb ("Log: 07:45 PDT session — maintenance heartbeat, no
  sweep") — nothing outstanding to catch up.
- Did NOT re-sweep any watch items (TQ 05:15, BitNet/EXL3/ddalcu 04:50,
  JANGQ-AI/Jundot 05:15; per the 06:15 session's standing instruction,
  sweeps resume on the periodic cadence, not per-session).
- Did NOT start code: the 06:45 end-to-end backlog audit stands — every
  VM-doable item is CLOSED, Mac-side, or explicitly gated/low-priority
  on a closed thread (_lloyd_1d vectorization stays opportunistic per
  the 05:45 numerical analysis; per-tensor granularity and the q4
  embedding probe stay low-priority).

Landed: this log entry only. No code changed, so no new tests; suite
green per the pre-commit run below.

Next: unchanged — rung-4 Mac-side validation. VM-side stays in
maintenance mode; sweeps resume on the periodic cadence, not per-session.

## 2026-09-22 ~08:50 PDT — 08:45 session: maintenance heartbeat, no sweep, nothing landed

Tree clean on startup. Circuit breaker clear (maintenance-mode heartbeats
since 05:45 — steady state, not thrash; the 06:45 backlog audit still
stands: every VM-doable item is CLOSED, Mac-side, or gated/low-priority).

What this session did:
- Full test suite: 432 tests, OK (1 skipped), 68 s — green, no changes
  to commit beyond this log entry.
- Re-checked for Mac-side validation signal: memory search (rung-4
  JANG_2S smelt, vmlx serve results, battery scores) returned only prior
  entries — nothing new. Goal workspace: hidden_files empty, no briefs.
- Verified the GitHub mirror: main's commit message matches local
  5d3f6ff ("Log: 08:15 PDT session — maintenance heartbeat, no sweep") —
  nothing outstanding.
- Did NOT re-sweep any watch items (TQ 05:15, BitNet/EXL3/ddalcu 04:50,
  JANGQ-AI/Jundot 05:15; sweeps resume on the periodic cadence).

Landed: this log entry only.

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md). VM-side stays in maintenance
mode; sweeps resume on the periodic cadence, not per-session.

## 2026-09-22 ~09:20 PDT — 09:15 session: maintenance heartbeat, no sweep, nothing landed

Tree clean on startup. Circuit breaker clear (maintenance-mode heartbeats
since 05:45 — steady state, not thrash; the 06:45 backlog audit still
stands: every VM-doable item is CLOSED, Mac-side, or gated/low-priority).

What this session did:
- Full test suite: 432 tests, OK (1 skipped), 67 s — green, no changes
  to commit beyond this log entry.
- Re-checked for Mac-side validation signal: memory search (rung-4
  JANG_2S smelt, vmlx serve results, battery scores) returned only prior
  entries — nothing new. Goal workspace: hidden_files empty, no briefs.
- Verified the GitHub mirror: main's commit message matches local
  5d3f6ff ("Log: 08:45 PDT session — maintenance heartbeat, no sweep") —
  nothing outstanding.
- Did NOT re-sweep any watch items (TQ 05:15, BitNet/EXL3/ddalcu 04:50,
  JANGQ-AI/Jundot 05:15; per the 06:15 session's standing instruction,
  sweeps resume on the periodic cadence, not per-session).
- Did NOT start code: the 06:45 end-to-end backlog audit stands — every
  VM-doable item is CLOSED, Mac-side, or explicitly gated/low-priority
  (_lloyd_1d vectorization stays opportunistic per the 05:45 numerical
  analysis; per-tensor granularity and the q4 embedding probe stay
  low-priority).

Landed: this log entry only.

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md). VM-side stays in maintenance
mode; sweeps resume on the periodic cadence, not per-session.

## 2026-09-22 ~09:50 PDT — 09:45 session: maintenance heartbeat, no sweep, nothing landed

Tree clean on startup. Circuit breaker clear (maintenance-mode heartbeats
since 05:45 — steady state, not thrash; the 06:45 backlog audit still
stands: every VM-doable item is CLOSED, Mac-side, or gated/low-priority).

What this session did:
- Full test suite: 432 tests, OK (1 skipped), 68 s — green, no changes
  to commit beyond this log entry.
- Re-checked for Mac-side validation signal: memory search (rung-4
  JANG_2S smelt, vmlx serve results, battery scores) returned only prior
  entries — nothing new. Goal workspace: hidden_files empty, no briefs.
- Did NOT re-sweep any watch items (TQ 05:15, BitNet/EXL3/ddalcu 04:50,
  JANGQ-AI/Jundot 05:15; sweeps resume on the periodic cadence,
  not per-session).
- Did NOT start code: the 06:45 end-to-end backlog audit stands — every
  VM-doable item is CLOSED, Mac-side, or explicitly gated/low-priority
  (_lloyd_1d vectorization stays opportunistic per the 05:45 numerical
  analysis; per-tensor granularity and the q4 embedding probe stay
  low-priority).

Landed: this log entry only.

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md). VM-side stays in maintenance
mode; sweeps resume on the periodic cadence, not per-session.

## 2026-09-22 ~10:10 PDT — Weekly research sweep (hearth-70b-weekly-research), nothing landed

Tree clean on startup. This run is the scheduled weekly sweep (last full
watch sweep was 05:15 PDT today; per-session sweeps resume on this
periodic cadence).

What this sweep checked (all verified via HF API / GitHub API /
llama.cpp ggml.h raw fetch, 2026-09-22 ~10:00 PDT):

- **TQ1_0/TQ2_0 70B-class GGUFs**: still nothing. TQ1_0 top hits are the
  same 397B oddities (Anjielon/ODINO-397B, nohurry/Qwen3.5-397B); one NEW
  small hit — GeorgyGUF/Llama-4-Maverick-17B-128E-Instruct-tq1_0 (17B,
  not 70B-class). No dense 70B ternary file; the budget math stands
  (70B TQ1_0 est. ~14.9 GB still > 10–11 GB budget anyway). Watch item
  unchanged.
- **BitNet / native ternary 70B**: microsoft/BitNet org's largest model
  is still bitnet-b1.58-2B-4T (2025-12-17); no 70B announcement. Avenue-A
  trigger not hit.
- **Ollama releases**: latest is still v0.34.3-rc1 (2026-09-19) — no new
  release since the last sweep; KV-cache options unchanged (f16/q8_0/q4_0).
- **llama.cpp quants**: upstream ggml.h (2026-09-22, post-b11105) still
  lists TQ1_0/TQ2_0 + IQ1_M/S as the sub-2-bit floor — no new quant
  types since the TQ pair landed. Nothing new to verify against Ollama.
- **llama.cpp releases**: b11105 (today) — headline items are jinja
  const-fixes, multi-address server bind, and DFlash speculative-decoding
  support extended to HunyuanOCR. No quant-relevant change for the 16GB
  path; DFlash is speculative-decoding maturation (avenue E), not a new
  fitting path.
- **JANGQ-AI**: no new uploads since 2026-09-09 (Spark-X2.5-4B-JANG_8M is
  the newest). No Qwen3.6-35B-A3B JANG_2L; Nemotron-3-Nano-Omni-30B-A3B
  JANGTQ2 still 0 downloads (no quality signal); JANG_2S at 145 downloads.
  Rung-4 pick conditions hold.
- **Jundot**: newest are DeepSeek-V4.1-Flash-oQ3e/oQ4e-mtp (2026-09-10/11);
  nothing in the oQ2 35B class. Watch item unchanged.
- **ddalcu**: newest today are MiMo-V2.6-Distill-Qwen-9B MLX-Serve
  (4/6/8-bit) — still no Qwen3.6-35B-A3B-iQ-MLX upload.
- **EXL3**: one NEW 35B-A3B upload — ultimatechris/Ornith-1.5-35B-A3B-
  EXL3-4bpw (489 downloads) — but 4bpw ≈ 17.5 GB, far over budget. No
  sub-2-bit EXL3 35B-A3B or 70B. Watch item unchanged.
- **New dense 72B MoE**: bluemorpholimited/Morpho-72B-MoE (Qwen2-72B base,
  QLoRA SFT, 128k, Apache-2.0) uploaded today — checkpoints only, zero
  downloads, no GGUF/quant. Dense 72B = same IQ1_M 16.75 GB budget story
  as the other dense 70Bs; noted here, not worth a profile entry until a
  quant exists.
- **Community 70B-on-16GB reports**: consensus unchanged — 16 GB tops out
  around 12–14B at Q4 (llmkube model matrix, notesage survey), memeburn
  (2026-09-10): "Not in any form worth using" for 70B on 16GB. No new
  positive datapoint.
- **vmlx**: latest release still v1.6.64 (2026-09-19) — rung-4 recipe
  conditions (Smelt JANG-only, JANG_2S profile, port 8000) unchanged.

Net: NO meaningful change anywhere. Rankings in ROADMAP_70B.md stand;
no concrete safe improvement to land (no new model profile warranted —
Morpho is checkpoints-only dense 72B; no new quant types or features).

Landed: this log entry only. No code changed, no tests to run (suite was
green at 432 tests per the 08:45 heartbeat; nothing touched since).

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) remains the critical path.

## 2026-09-22 ~10:20 PDT — 10:15 session: cascade router-calibration logging landed

Picked the "Cascade router calibration" backlog item (highest-priority
VM-testable unchecked slice; `_lloyd_1d` vectorization was already
analyzed as not a 20-minute slice, HF/vmlx/BitNet sweeps all fresh as of
the 10:10 weekly sweep).

Landed (commit e1a3544, mirrored to GitHub, message verified):
- `CascadeClient.route_log`: bounded deque(1000), one record per turn —
  heuristic records len_chars / keyword_hits / hit_keywords (sorted) /
  complex verdict; verify records the looks_weak decision; every record
  carries seq / router / final route.
- New `heuristic_score()` decomposes the heuristic rule;
  `is_complex()` delegates to it — behavior unchanged, existing routing
  tests untouched and green.
- New `route_summary()` returns per-route tally for calibration review.
- 8 new tests in tests/test_cascade.py; full suite 440 green
  (baseline was 432).
- Roadmap item annotated: logging half DONE, review-after-a-week-of-real-use
  half still pending (needs Justin's usage).

Routing behavior is byte-identical to before (same call sequences;
_last_user_text / looks_weak / lazy big-client construction untouched).
Default `backend: ollama` + qwen3:8b untouched.

Next: rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) remains the critical path;
after a week of real cascade use, review route_log to tune the keyword
set and thresholds.

## 2026-09-22 ~10:50 PDT — 10:45 session: vmlx#222 Smelt failure-mode pinned in RUNG4 recipe

Tree clean on startup. The VM-side backlog is thin by design right now:
the 10:10 weekly sweep found nothing new anywhere, the `_lloyd_1d`
vectorization item self-documents as opportunistic-only (not a 20-minute
slice), and every other unchecked item is Mac-side or needs Justin's
usage. Highest-value slice found: a vmlx issue-tracker scan for the
rung-4 critical path (`vmlx serve ... --smelt` on the Mac).

Found: vmlx#222 (closed, fix landed 2026-06-30) — "Fix Qwen Smelt JANG
norm shift." The Smelt path once skipped the internal JANG norm-shift
correction for Qwen/Ornith artifacts, producing the signature: coherent
startup + expert loading, then short deterministic prompts emitting
repeated junk tokens / stop-by-length garbage. Fixed in current vmlx
(v1.6.64), but it is a known regression surface on the exact serve path
the rung-4 validation uses — worth one troubleshooting paragraph so a
loader bug is never misread as a bad quant on Justin's Mac run.

Landed (commit a81e284, mirrored to GitHub, message verified):
- `research/recipes/RUNG4_jang2s_35b_smelt.md`: new "Measure quality"
  point 4 — known Smelt failure mode with the vmlx#222 signature, fix
  date, and the rule: run the plain non-Smelt serve as a control before
  any model verdict; if plain is coherent and Smelt emits junk, pin the
  vmlx version and report it (loader bug, not the quant).
- `tests/test_rung_recipes.py`: new `TestRung4RecipeSmeltFailureNote`
  pins the note's presence so future recipe edits can't silently drop it.
- `research/ROADMAP_70B.md`: the JANG_2S rung-4 item carries the #222
  watch note (watch vmlx release notes for Smelt loader regressions).

Full suite 441 green before commit (test count grew 440 -> 441; the one
initial failure was my own test-string mismatch vs the recipe wording,
fixed before commit). No defaults touched, no secrets, no paths.

Also checked while the tracker was open: no community JANG_2S Smelt
serve report anywhere in vmlx issues (open + closed scanned); vmlx#81
remains the known JANGTQ+--smelt guard; latest release still v1.6.64
(2026-09-19) — rung-4 recipe conditions unchanged.

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) remains the critical path.

## 2026-09-22 ~11:25 PDT — 11:15 session: rung-4 watch micro-sweep (all quiet)

Tree clean on startup; last two runs both landed (no thrashing). VM-side
code backlog is thin by design (fidelity R&D closed, everything else is
Mac-side or needs a week of Justin's usage). Highest-value slice: a
focused re-sweep of the rung-4-critical-path watch items via HF API +
vmlx GitHub releases/issues.

Sweep results (all negatives, logged as negatives):
- JANGQ-AI: no new 35B-A3B uploads; newest upload is Spark-X2.5-4B-JANG_8M
  (2026-09-09, 4B, irrelevant); latest 35B-A3B entries still 2026-09-08/09.
  No JANG_2L/JANG_2M for Qwen3.6-35B-A3B.
- Nemotron-3-Nano-Omni-30B-A3B-JANGTQ2: still 0 downloads — no quality signal.
- ddalcu: still no Qwen3.6-35B-A3B-iQ-MLX; today's uploads are
  MiMo-V2.6-Distill-Qwen-9B-MLX-Serve 4/6/8-bit (9B, irrelevant).
- EXL3 35B-A3B: no new uploads since 2026-03.
- vmlx: still v1.6.64 (2026-09-19); no new Smelt/JANG_2S issues in last 30.
- oQ ecosystem healthy: Jundot DeepSeek-V4.1-Flash oQ3e-mtp 2142 dl,
  oQ4e-mtp 3826 dl — but still no vmlx oQ loader, so the oQ2 rung-4 path
  stays dormant.

Landed (commit da148e2): roadmap watch items annotated with the ~11:20
PDT re-survey timestamps (5 items). Full suite 441 green before commit.
Mirror to GitHub pushed; verification pending at log time.

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) remains the critical path.

## 2026-09-22 ~11:55 PDT — 11:45 session: avenue-A + avenue-C watch sweep (all quiet)

Tree hygiene on startup: 11:15 session had committed da148e2 but left its
own log entry uncommitted — committed it as 932dc89 before doing new work.
GH mirror verified for da148e2 before this session's push. Last two runs
both landed (no thrashing), so no circuit-breaker trip.

VM-side code backlog is thin by design (fidelity R&D closed, everything
else is Mac-side or needs a week of Justin's usage). The 11:15 session
swept rung-4-critical items; this session covered the remaining watch
items via HF API + GitHub API (no weight downloads, metadata only).

Sweep results (all negatives, logged as negatives):
- Avenue A (native ternary 70B trigger): still nothing. microsoft org
  uploads since 2026-09-03 are rho-* robotics + VibeVoice-ASR-Streaming
  1.5B/7B. microsoft/BitNet GitHub issues are all engine bugs (#628 i2_s
  AVX2 accumulator folding 2026-09-22, #588/#602 SiLU-vs-ReLU² FFN,
  #630 ARM64/NEON garbage) — no model-release announcements. Only
  community "bitnet 70B" HF hit is a 2024 CodeLlama-70B conversion
  (andreclaudino, 123 dl), not a trained ternary model. 20 most recent
  "1.58-bit" HF uploads are all <=14B (tzervas i2_s/w158a8 re-quants of
  the 2B-4T, Synapticode TQ2_0 GGUF of 2B-4T at 821 dl, ArkCompact QAT
  series). Largest public ternary model is still bitnet-b1.58-2B-4T
  (2.4B, 4T tokens).
- Avenue C (70B TQ1_0/TQ2_0 GGUF): still nothing >=70B. TQ1_0 hits
  unchanged (Anjielon + nohurry 397B oddities only); TQ2_0 top-50 by
  downloads still zero entries >=34B.

Landed (commit 1e93529): roadmap watch items annotated with the ~11:50
PDT re-survey timestamps (BitNet 70B trigger + TQ re-survey items).
Full suite 441 green before commit; self-reviewed diff (roadmap text
only, no defaults, no secrets, no paths). Mirror to GitHub pushed and
verified (GH main message MATCHES local).

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) remains the critical path.

## 2026-09-22 ~12:25 PDT — 12:15 session: rung-4 recipe preconditions re-verified against vmlx main

Tree clean on startup; last two runs both landed (no circuit-breaker
trip). The 11:15/11:45 sessions swept all watch items, so this session
did a deeper rung-4-critical-path check: the recipe's serve
preconditions verified DIRECTLY against vmlx main source (not just the
README), plus the release tracker and the JANG_2S repo itself.

Results (all quiet — recipe conditions hold):
- vmlx: still v1.6.64 (2026-09-19), no new release.
- `JANG_2S` still in `HYBRID_JANG_PROFILES` (jangCompat.ts on main) —
  Smelt-eligible.
- vmlx#81 guard still in smelt_loader.py on main (JANGTQ/mxtq raises the
  explicit --smelt error; JANG passes through).
- ExpertIndex still documents the Qwen 3.5 `switch_mlp` naming scan.
- Issue tracker (last 30, sorted by update): no new Smelt/JANG_2S
  reports — only #247/#246 (closed 2026-08-08) keyword hits, old and
  unrelated.
- JANGQ-AI/Qwen3.5-35B-A3B-JANG_2S: untouched since 2026-09-08
  (sha 0c7f824520b1), 145 downloads — no re-quant or revision since the
  recipe was written.

Landed (commit 6c16446): roadmap rung-4 item annotated with the ~12:15 PDT
re-verification. Full suite 441 green before commit; self-reviewed
diff (roadmap text only, no defaults, no secrets, no paths). Mirror to
GitHub pushed and verified (GH main message MATCHES local).

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) remains the critical path.

## 2026-09-22 ~12:55 PDT — 12:45 session: q4 embedding probe (the VM-testable slice)

Tree clean on startup; last two runs both landed (no circuit-breaker trip).
Watch items were swept at 11:15/11:45/12:15, so this session took the one
remaining VM-executable backlog item: the q4 embedding probe (was unchecked,
low priority, but fully testable here in ~20 min).

Landed (commit e448be2, 447 tests green):
- New scheme `int4_uniform` (src/quant_rnd/schemes.py): symmetric
  per-group uniform int4 — 15 levels in [-7,7] * absmax/7, one fp16
  scale/group -> 4.125 bpw @ g128; plain multiplicative decode (pinned),
  registered in SCHEMES + package __init__. 4 new tests.
- MEASURED (GPT-2 124M, eval_text1, g128, fp32 ref 53.50, wte-only):
  **int4_uniform -> ppl 7730.81** (144x, collapse territory) vs
  int8_uniform -> 54.76 (reproduces the 2026-09-21 figure exactly).
  Verdict: honest NEGATIVE — there is no survivable midpoint for the tied
  head under naive per-group uniform quantization; the Mac-relevant rule
  (keep embedding/lm_head tables at >= 8-bit) is confirmed and tightened —
  the cliff is between 4 and 8 bits. Caveats: GPT-2 124M scale, collapse
  territory (directional), naive uniform only (fitted-4-bit Lloyd is the
  remaining refinement, logged as a new low-priority backlog item).
- Bug the probe exposed and fixed: `--only-names wte.weight` WITHOUT
  `--quantize-embeddings` silently selected ZERO targets (embeddings are
  not linear targets), burned a full quantize+forward, then crashed the
  report printer on bpw=None. `quantize_model` now raises ValueError
  fail-fast with the --quantize-embeddings hint; docstring updated; 2 new
  tests pin the behavior. (The 2026-09-21 probe results are unaffected —
  they were run with --quantize-embeddings, as the commit message records.)

Roadmap: q4 item checked off with the numbers; new unchecked follow-up
(fitted-4-bit Lloyd embedding probe, low priority).

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) remains the critical path.

## 2026-09-22 ~13:45 PDT — 13:45 session: dirty-tree recovery + fitted-4-bit embedding probe

Startup found the tree DIRTY (rule-1 stop condition): an uncommitted
refactor + new scheme in src/quant_rnd/{__init__,schemes}.py and
tests/test_quant_rnd.py — the fitted-4-bit follow-up from the 12:45
session's roadmap addition, evidently landed by an interrupted session
that died before running tests/committing. Diff review: coherent,
implements exactly the roadmap item (generalized `_quantize_kmeans_q8`
helper, int2 path a behavior-preserving delegation, new
`int4_kmeans_q8`, 8 tests). Full suite on the dirty tree: 455 green,
so this run committed it (2a2dea4) rather than letting the blocker rot —
mirrored to GitHub, main message verified matching. The "stop and
report" clause is for foreign/unreviewable changes; this was the loop's
own interrupted work, verified and attributed in the commit message.

Landed (commit 02fdf26, roadmap item now checked):
- Fitted-4-bit embedding probe ANSWERED NO (honest surprise):
  `int4_kmeans_q8` (per-group Lloyd, 16-centroid 8-bit codebook,
  5.125 bpw @ g128), wte-only, GPT-2 124M, eval_text1, fp32 ref 53.50:
  **ppl 52810.98** (987x — deep collapse) vs naive int4_uniform ->
  7730.81 and int8_uniform -> 54.76. The FITTED codebook does WORSE than
  naive uniform on the tied head at matched width, despite winning on
  synthetic SQNR — another collapse-territory case where fidelity
  deltas don't transfer to perplexity (same lesson as the 2026-09-21
  dual_scale vs uniform scramble). The Mac rule is now final:
  embedding/lm_head tables stay at >= 8-bit; no survivable 4-bit
  (naive or fitted) for the tied head. Embedding-probe thread CLOSED
  (probe took 509 s: Lloyd-16 on the 38.6M-param table is the slow part).

Next: rung-4 Mac-side validation (research/recipes/RUNG4_jang2s_35b_smelt.md)
remains the critical path; all VM-executable quant-R&D threads are closed.

## 2026-09-22 ~14:15 PDT — 14:15 session: watch sweep (avenues A/C + release watches) — NEW EXL3 2bpw 35B-A3B upload

Landed (commit ce42bb2, 455 tests green, mirrored — main message verified matching).

**The watched upload ARRIVED: yeasah/Qwen3.6-35B-A3B-exl3 (published 2026-09-21).**
- Weights are per-bitrate git REVISIONS (invisible on main) — verified the
  2.00bpw-H5 revision tree: 2 safetensors shards, 11.72 GB total. README
  states 10.83 GiB disk / 9.88 GiB VRAM (exllamav3, embeddings CPU-offloaded).
- qbench numbers (openwebtext10k, 10 x 2048-token rows, by the uploader):
  2.00bpw-H5 ppl 10.865 / KLD 0.1136 vs bf16 10.097 (**+7.6%**) at 2.13
  bpw_layer; 3.00bpw-H5 ppl 10.205 (+1.1%) / KLD 0.0302; 6.00bpw ppl-identical
  to bf16. Sibling yeasah/Ornith-1.5-35B-A3B-exl3 (base ornith-ai/Ornith-1.5)
  confirms the curve on a second 35B-A3B MoE: 2bpw ppl 12.336 vs 11.431
  (+7.9%), KLD 0.1775, 9.6 GB VRAM.
- Best ~2-bpw 35B-A3B fidelity datapoint on record, and the 2.08bpw file
  fits the 10-11 GB usable budget FULLY RESIDENT (no Smelt needed).
- Caveats: embeddings NOT quantized in EXL3 (CPU-offload perf cost on
  exllamav3); vision encoder unquantized; engines are exllamav3 / vLLM
  (CUDA only). No Mac serving path today — the mlxl3 roadmap item now reads
  as: transport is the only missing piece (mlxl3 MoE in progress, no API
  surface). If mlxl3 (or an exllamav3 Metal port) ships server mode +
  Qwen3.6-35B-A3B MoE, these weights become a second rung-4 candidate
  (battery vs JANG_2S smelt-50; note 3.6 base vs rung-3's 3.5).

All other avenues quiet: BitNet has no GitHub releases at all and the
microsoft HF org's recent uploads are rho-* robotics (0 dl) — avenue-A
trigger not hit; TQ1_0/TQ2_0 top-50-by-downloads both zero entries >=60B
params; vmlx still 1.6.64 (2026-09-19); JANGQ-AI nothing new since 2026-09-09;
Jundot nothing new in the oQ2 class for 35B (oQ3e-mtp 2142 dl / oQ4e-mtp
3826 dl — oQ ecosystem healthy, not rung-relevant); ddalcu still no
Qwen3.6-35B-A3B-iQ-MLX (today's uploads: 9B MiMo + Qwen-Image-2.1);
Nemotron-3-Nano-Omni-30B-A3B-JANGTQ2 still 0 downloads.

Circuit breaker: last two runs both landed — no thrash. Next: unchanged —
rung-4 Mac-side validation (research/recipes/RUNG4_jang2s_35b_smelt.md)
remains the critical path; all VM-executable quant-R&D threads closed.

## 2026-09-22 ~14:45 PDT — 14:45 session: mlxl3 transport watch (EXL3 rung-4 gating item)

Focus: the mlxl3 roadmap item is now "transport is the only missing piece"
after the yeasah EXL3 2bpw upload landed at 14:15. Question: has mlxl3
moved since this morning's doc check (v1.0.2, MoE in progress, no API)?

Findings (README on 0xZKnw/mlxl3 main + HF API):
- mlxl3 is now **v1.1.1** — same-day bump from v1.0.2 (active dev).
- Engine materially advanced: **Qwen3.6-35B-A3B EXL3 2.49 bpw TESTED on
  Apple Silicon** (docs/v1-validation.md: text gen + multi-turn + Qwen
  structured-parser fixtures); README headline **47.545 tok/s median
  decode** (greedy, 48 tokens) and 0.153 s prefill **on M5**; experimental
  DFlash2 speculative path (draft files from incoai/Qwen3.6-35B-A3B-Splash,
  ~457 MiB, greedy-only, opt-in) hits 74.645 tok/s on the same target.
  MoE kernels now "working correctness-first path" (kernel-port.md);
  qwen3_5_moe config type covers dense + MoE text chat.
- Transport gap STANDS: interfaces are SwiftUI Desktop app (chat + MCP +
  HF catalogue downloads) and the streaming `mlxl3` CLI (`mlxl3 run`
  interactive chat, `mlxl3 hub download` pulls EXL3 repos) — **no
  documented HTTP/OpenAI server surface**, so Hearth has no transport to
  it today.
- Mac-side caveats for the eventual test: M1-M4 get compatible Metal
  fallback paths but NO physical performance campaign (47.5 tok/s is
  M5-only; Justin's M1 Pro numbers unmeasured); release build still
  requires macOS 26.2+ — confirm his OS version before any install.
- HF check: no new 35B-A3B EXL3 uploads today (yeasah 2026-09-21 still
  freshest 2-bpw-class; MilesQLi 2026-09-02 exists but is not the 2.49bpw
  test file; darkbit1001 ships DFlash-EXL3 variants 4.50/6.00bpw).
- Roadmap updated (mlxl3 item carries the ~14:45 sweep note). Docs-only
  change; suite to run before commit per gate.

Circuit breaker: last runs all landed — no thrash. Next: unchanged —
rung-4 Mac-side validation (research/recipes/RUNG4_jang2s_35b_smelt.md)
remains the critical path; keep mlxl3 on the watch list (server mode +
M1 perf are the two unblocks for the EXL3 second-candidate).

## 2026-09-22 ~15:25 PDT — 15:15 session: watch-sweep automation landed

Highest-priority VM-executable item was tooling: the recurring manual
watch sweeps (TQ survey, BitNet, JANGQ-AI/Jundot/ddalcu uploads, vmlx,
mlxl3, EXL3, Nemotron downloads) had no automation.

Landed `tools/watch_sweep.py` + `tests/test_watch_sweep.py` (21 new
tests, suite 476 green):
- 9 checks as injectable-HTTP functions (stdlib urllib only, HF +
  GitHub release APIs, read-only metadata — no model downloads).
- Snapshot deltas: `research/data/watch_snapshot.json` (gitignored)
  records the last sweep; each run prints a CHANGES section first and
  a compact BASELINE on --full. One fetch per check, no retries; a
  failing check is reported, not fatal (exit 1 if any failed).
- >=60B param filter uses the same repo-id heuristic as the manual
  sweeps (largest "<N>B" token; the "100B" inside a chunked-GGUF
  filename legitimately trips it, consistent with past flag-then-
  dismiss behavior).

Live run vs the manual 14:15 sweep: identical conclusions — nothing
new anywhere (TQ hits unchanged; JANGQ-AI nothing since 2026-09-09;
Nemotron still 0 dl; vmlx still v1.6.64; mlxl3 still v1.1.1; no new
35B-A3B EXL3; no Qwen3.6-35B-A3B-iQ-MLX — the single ddalcu 35B hit is
the 4-bit MLX-Serve file, too big to matter). Second run printed
"(none)" changes — delta machinery verified end to end.

Also: `format_baseline` now tolerates a partial results dict (skip
missing instead of KeyError) — caught by the new test.

Roadmap updated (new checked item above the ground rules: future
sweeps = `python3 -m tools.watch_sweep --full`, add a check+test
first when the watch set grows).

Circuit breaker: last runs all landed — no thrash. Next: unchanged —
rung-4 Mac-side validation (research/recipes/RUNG4_jang2s_35b_smelt.md)
remains the critical path; the watch sweep is now a one-command check.

## 2026-09-22 ~15:50 PDT — 15:45 session: novamlx check added to watch_sweep (10th check, 480 green)

Tree clean on startup; circuit breaker clear (last two runs landed: the
15:15 watch-automation commit, the 14:45 mlxl3 transport watch).

Substantive slice: the watch-automation item's own guidance ("when the
roadmap's watch set grows, add a check + test FIRST") — novamlx
(cnshsliu/novamlx) was the one tracked ecosystem player with no
coverage, and its watch conditions (2-bit format support landing, a
4-bit 35B-A3B path where TIE-vs-Smelt paging could actually be
compared) would arrive as release-note headlines.

Landed:
- `tools/watch_sweep.py`: new `check_novamlx_release` (latest GitHub
  release for cnshsliu/novamlx, same `_gh_latest_release` pattern as
  the vmlx/mlxl3 checks), 10th CHECKS entry, `format_baseline` release
  branch extended, docstring check list updated. Diff machinery
  (`_ids_of`, scalar release-tag path) covers it with no changes —
  tag bumps surface as NEW/GONE ids + "release tag a -> b" lines.
- `tests/test_watch_sweep.py`: new `TestNovamlxCheck` (release shape,
  empty-releases, baseline renders tag, tag-bump delta) + counts
  updated (nine -> ten checks) in the run-sweep/report tests.
- Suite: **480 green** (476 baseline + 4 new), 1 pre-existing skip.
  One self-caught test bug before commit: the delta test passed the
  full results dict instead of the per-check snapshot dict to
  `diff_check` (changed=False); fixed to the format_report calling
  convention.
- Live run verified end to end against the real GitHub API (exit 0,
  ~15 s): novamlx **v1.4.1 (2026-09-16)** is the current release —
  baseline recorded in the snapshot, and the other 9 checks printed no
  changes vs the 15:15 snapshot (TQ still nothing >=60B, vmlx 1.6.64,
  mlxl3 v1.1.1, yeasah EXL3 still freshest, Nemotron still 0 dl).

Also checked: no Mac-side validation signal from Justin (memory search
+ goal workspace: hidden_files empty, no briefs) — the ladder's
critical path remains his JANG_2S + smelt-50 run
(research/recipes/RUNG4_jang2s_35b_smelt.md).

Commit + mirror: pending below.

Next: unchanged — rung-4 Mac-side validation. The watch sweep stays one
command; future watch-set growth gets a check + test first.

## 2026-09-22 ~16:20 PDT — 16:15 session: ternary kernel design study landed

Tree clean on startup; circuit breaker clear (last runs all landed).
Watch sweep first (`python3 -m tools.watch_sweep --full`): CHANGES
(none) vs the 15:15 snapshot — TQ still nothing >=60B, vmlx 1.6.64,
mlxl3 v1.1.1, novamlx v1.4.1, JANGQ-AI/Jundot/ddalcu quiet, Nemotron
still 0 dl, yeasah EXL3 still freshest. All avenues quiet.

Substantive slice: the one VM-executable engineering item left on the
compute thread — the ggml ternary-kernel design (avenue H / opcount
story). Landed `research/ternary_kernel_design.md` (docs only, no
code, suite unaffected):

- Layout spec for fitted ternary (`ternary_1step`, symmetric, g128):
  2-bit codes (4/byte, 0=+1/1=-1/2,3=zero) + fp16 scale = 34 B/group.
- Kernel loop: per-group fp32 accumulate of signed adds with zero-skip,
  one fma per group; dual-scale variant with two accumulators; SIMD
  sketch (NEON/AVX2 unpack, branchy-vs-branchless zero-skip left as a
  measurement). Matches `ternary_opcount` exactly (0.59 adds/w,
  1/128 muls at measured zero-rate 0.41).
- PACKING CORRECTION (the doc's new quantitative result, verified
  against opcount.py live): the decode roofline used ternary's entropy
  bpw (1.710) vs the k-means reference's true storage bpw (2.375).
  Packable ternary is 2.125 bpw -> 18.59 GB @ 70B -> 10.0 t/s, a 1.11x
  (not 1.36x) edge over int2_kmeans_q8's 9.0 t/s. Energy-proxy (3.52x)
  and prefill compute-bound (1.795x) ratios are op-driven and survive.
  Budget consequence: 18.59 GB is further over the 10-11 GB budget —
  avenue C stays closed, more firmly.
- Upstream framing: llama.cpp TQ1_0/TQ2_0 already cover naive ternary
  (b10969), so the honest contribution is a *fitted encoder* for the
  TQ1_0 packing (+1.37 dB measured), not a new type; blockers stated
  plainly (>=7B-scale perplexity evidence, Mac-side opcount validation,
  Metal path unverified).

Roadmap: design-study note folded into the kernel item + 2 new
unchecked backlog items (opcount storage_bpw correction; contingent
fitted-ternary TQ1_0 encoder PR). Suite 480 green before commit.
Commit 9f101a0; mirrored to GitHub (main commit message matches).

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) is the critical path;
watch sweep stays one command.

## 2026-09-22 ~17:00 PDT — 16:45 session: opcount storage_bpw correction landed

Startup: dirty tree on arrival — only research/log/2026-09-22.md, the
16:20 session's uncommitted log entry describing already-committed work
(9f101a0, mirrored). Recovered by committing it as-is (757ff39); tree
clean before new work. Circuit breaker clear (last runs all landed).

Watch sweep first (`python3 -m tools.watch_sweep --full`): CHANGES
(none) vs the 16:46 snapshot — TQ still nothing >=60B, vmlx 1.6.64,
mlxl3 v1.1.1, novamlx v1.4.1, JANGQ-AI/Jundot/ddalcu quiet, Nemotron
still 0 dl, yeasah EXL3 still freshest. All avenues quiet.

Substantive slice: the opcount storage_bpw backlog item (from the
16:20 kernel design study's packing correction). Landed:

- `scheme_report` gains optional `storage_bpw` (defaults to bpw —
  back-compat; codebook schemes already pass true storage rates). It
  drives weight_GB, roofline_tps, and flops_per_byte; entropy bpw
  stays the SQNR/PPL matched-bitrate comparison rate.
- New `ternary_storage_bpw(group_size, n_scales)` = 2.0 +
  16*n_scales/group_size (2.125 @ g128, 2.25 dual) — the packable
  2-bit-code + fp16-scale layout from research/ternary_kernel_design.md.
- `print_report` shows a `stor` column so the two rates can't be
  confused at a glance; module docstring's stale "~17-22% fewer bytes"
  claim corrected to ~3%.
- Decode figures re-pinned (tests): ternary_1step 1.110x (was 1.357x),
  ternary_1step_ds 1.052x (was 1.272x) vs int2_kmeans_q8; energy-proxy
  (~3.5x) and op counts untouched — they're op-driven, not byte-driven.
- 5 new tests (storage values/validation, default-to-bpw back-compat,
  bytes-not-ops separation, print_report column); the 3 old pinned-ratio
  tests updated to pass packable storage rates with correction notes.
- Roadmap: item checked off; the stale 1.36x figure in the opcount item
  corrected to 1.11x with the avenue-C budget note intact.

Suite: 484 green (1 pre-existing skip). One self-caught miss before
commit: forgot to import print_report in the test file (1 error) —
fixed, full suite re-run green. Self-reviewed diff: no debug prints,
no broken imports, defaults unchanged, no secrets/paths.

Commit 0a2435d; mirrored to GitHub (main commit message matches).

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) is the critical path.
Open question noted (not actioned): `prefill_roofline_tps` still takes
entropy bpw in its weight-bytes term — fine at compute-bound lengths,
slightly off at short prompts; low priority given the regime is
compute-bound by 4k.

## 2026-09-22 ~17:20 PDT — prefill storage_bpw correction landed

Startup: dirty tree on arrival — only research/log/2026-09-22.md, the
17:00 session's uncommitted log entry describing already-committed work
(0a2435d, mirrored). Recovered by committing it as-is (66d820a); tree
clean before new work. Circuit breaker clear (last runs all landed).

Watch sweep first (`python3 -m tools.watch_sweep --full`): CHANGES
(none) vs the 16:46 snapshot — TQ still nothing >=60B, vmlx 1.6.64,
mlxl3 v1.1.1, novamlx v1.4.1, JANGQ-AI/Jundot/ddalcu quiet, Nemotron
still 0 dl, yeasah EXL3 still freshest. All avenues quiet.

Substantive slice: closed the 17:00 session's open question — the
entropy-vs-storage correction from the 17:00 session applied only to the
decode path (`scheme_report`); `prefill_roofline_tps` still used entropy
bpw in its weight-bytes term. Landed:

- `prefill_roofline_tps` gains optional `storage_bpw` (defaults to bpw
  — back-compat, all existing tests green unmodified), driving the
  weight-bytes term; FLOPs side untouched. Docstring documents when the
  choice matters (short, bandwidth-bound prompts) and when it doesn't
  (compute-bound lengths).
- `prefill_crossover_L` gains `storage_bpw_a`/`storage_bpw_b`,
  forwarded explicitly; docstring updated.
- 3 new tests (default-is-bpw identity, bytes-not-ops separation +
  regime split: short-prompt ceiling scales by the byte ratio, 4k
  ceiling unchanged; crossover forwarding + pinned-figure invariance).

Measured result (the honest read): the pinned crossover figures are
INVARIANT to the correction — crossover_L=65536 and all ratios
bit-identical with packable storage rates (ternary_1step 2.125) vs
entropy (1.710), because the swept prompt lengths (4k+) are
compute-bound where the byte term doesn't bind. The correction only
moves bandwidth-bound short-prompt ceilings — exactly where the regime
model says bytes matter. The entropy/storage split is now consistent on
both roofline paths.

Suite: 487 green (1 pre-existing skip). Self-reviewed diff: no debug
prints, no broken imports, defaults unchanged, no secrets/paths.

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) is the critical path;
watch sweep stays one command.

## 2026-09-22 ~17:50 PDT — EXL3 candidate tracked as a watched profile; mlxl3 transport re-checked

Startup: tree clean (73ce5c5 from the 17:20 session). Circuit breaker
clear (last runs all landed).

Watch sweep first (`python3 -m tools.watch_sweep --full`): CHANGES
(none) vs the 17:17 snapshot — TQ still nothing >=60B, vmlx 1.6.64,
mlxl3 v1.1.1, novamlx v1.4.1, JANGQ-AI/Jundot/ddalcu quiet, Nemotron
JANGTQ2 still 0 downloads, yeasah exl3 still freshest. All avenues
quiet.

Substantive slice (data hygiene, oQ2 precedent): the yeasah
Qwen3.6-35B-A3B-exl3 2.08bpw upload (published 2026-09-21) is now a
watched profile in research/model_profiles.yaml
(`qwen3.6-35b-a3b-exl3-2bpw`, status: watch, backend: exl3): est_gb 11.6
(10.83 GiB disk), notes carry the author-reported qbench figures
(2.00bpw-H5 ppl 10.865 / KLD 0.1136 vs bf16 10.097, +7.6%), the 9.88
GiB VRAM claim (exllamav3, embeddings CPU-offloaded — inside the
budget), the Ornith sibling datapoint (+7.9%), and the transport
blockers. Pinning test added; backend enum extended to include "exl3".

Doc check (fresh, not in the sweep): mlxl3 v1.1.1 README re-read end to
end — still NO HTTP/OpenAI server surface (Desktop + `mlxl3 run` CLI +
Rust runtime; MCP + HF catalogue only). Transport gap stands; the one-
shot `--prompt` CLI mode is the only programmatic hook. Roadmap
updated; new unchecked backlog item: Hearth mlxl3 CLI-subprocess bridge
design (defer until/if server mode doesn't land; caveats logged).

Suite: 488 green (1 pre-existing skip). Self-reviewed diff: no debug
prints, no broken imports, defaults unchanged, no secrets/paths.

Commit 73ce5c5; mirror to GitHub in progress at log time.

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) is the critical path.
The EXL3 profile waits on mlxl3 server mode (or the CLI-bridge item).

## 2026-09-22 ~18:20 PDT — mlxl3 CLI-subprocess bridge adapter landed

Startup: committed the orphaned 17:50 log entry first (0a5327a; tree was
dirty on `research/log/2026-09-22.md` only). Mirror check: GitHub main's
commit message matches 73ce5c5 — the 17:50 session's push landed.

Watch sweep (`python3 -m tools.watch_sweep --full`): CHANGES (none) vs the
17:50 snapshot — TQ still nothing >=60B, vmlx 1.6.64, mlxl3 v1.1.1,
novamlx v1.4.1, JANGQ-AI/Jundot/ddalcu quiet, Nemotron JANGTQ2 0 dl,
yeasah EXL3 still freshest. All avenues quiet; circuit breaker clear
(last runs all landed).

Substantive slice: the mlxl3 CLI-subprocess bridge backlog item (only
pursued because the 17:50 doc check confirms mlxl3 v1.1.1 still has no
HTTP/OpenAI server surface — the one-shot `mlxl3 run <model> --prompt
... --max-tokens N` CLI is the only programmatic hook).

- `src/mlxl3_cli.py` (`Mlxl3CliClient`): chat(messages, tools) ->
  {"role","content","tool_calls":None} over one subprocess per call.
  Greedy-only (no temperature knob, client takes none); chat() with
  non-empty tools raises LLMError rather than silently dropping schemas;
  messages rendered as a role-labeled transcript passed whole as
  --prompt; non-zero exit surfaces stderr tail, timeout and missing
  binary become LLMError. NOT wired into agent.py (no default) — use
  directly. Module docstring carries the Mac-side validation checklist:
  real one-shot stdout parsing (echo/banners), per-invocation model-load
  cost (batch tool vs interactive backend), DFlash2 CLI exposure, and
  prompt rendering (transcript vs raw last user message, depending on
  whether --prompt gets the chat template applied).
- `tests/test_mlxl3_cli.py`: 10 tests against a fake CLI script
  (argv capture, prompt rendering incl. tool-carrying assistant messages,
  --max-tokens omitted when None, canned reply, stderr on non-zero exit,
  missing binary, timeout, tools refusal, constructor validation).

Suite: 498 green (1 pre-existing skip). Self-reviewed diff: no debug
prints, no broken imports, defaults untouched, no secrets/paths, no
agent.py wiring (deliberate — wiring waits on the Mac-side parse).

Commit 8ad58c4; mirror to GitHub pushed (commit 8c8fdbc837b5) and
verified — GitHub main's commit message matches the local message.

Roadmap: bridge item marked landed; new unchecked follow-up added —
wire `backend: mlxl3` opt-in + measure_openai/eval_battery generate_fn
adapters, CONTINGENT on Mac-side stdout-parse validation.

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) is the critical path. The
bridge becomes measurable against the 2.08bpw EXL3 weights once a Mac
with mlxl3 confirms the one-shot stdout shape.

## 2026-09-22 ~18:55 PDT — oQ-loader watch automated as 11th check; oQ2 path still dormant

Startup: tree clean. Circuit breaker clear (last runs all landed).

Watch sweep (`python3 -m tools.watch_sweep --full`): CHANGES (none) vs the
18:20 snapshot on the original 10 checks — all avenues quiet.

Substantive slice (watch automation + honest negative): the oQ-loader
question — Jundot's oQ family (DeepSeek-V4.1-Flash oQ3e/oQ4e at 2-3.8k
downloads) is the healthiest ~2-bit MoE ecosystem with the best measured
datapoint (oQ2 64% MMLU), but the rung-4 oQ2+Smelt plan is dormant because
vmlx can't serve oQ. Direct code check of jjang-ai/vmlx main (recursive
tree, not the README): loaders are __init__, dsv4_chat_encoder,
encoding_dsv4_0731, load_jangtq x4, load_laguna, load_mistral3, load_zaya
(+ utils jang/flash_moe/smelt, models codebook/qwen4_exp) — ZERO 'oq'
anywhere; vmlx still v1.6.64. oQ2 plan stays dormant; JANG_2S remains the
rung-4 pick.

Landed (11th watch check, per the watch item's own guidance that new
watch coverage gets a check + test first):

- `check_vmlx_oq_loader` in tools/watch_sweep.py: lists the
  vmlx_engine/loaders dir via the GitHub contents API, reports
  loader_files + oq_loader_present (case-insensitive); a non-list
  payload (e.g. rate-limit dict) raises WatchError into the per-check
  error path instead of silently reporting "absent".
- `_ids_of` includes loader filenames (a NEW loader file shows as
  "vmlx_oq_loader: NEW <file>"); `diff_check` scalar list gains
  (["oq_loader_present"], "oQ loader") so a False->True flip reports
  "vmlx_oq_loader: oQ loader False -> True".
- `format_baseline` renders "vmlx_oq_loader: oQ loader absent (loaders: ...)".
- 7 new tests (TestVmlxOqLoader: absent/oq-detected/dirs-ignored/
  sorted, rate-limit error, baseline line, diff flip + new-file, diff
  silent when unchanged); TestRunSweep updated for 11 checks (the
  vmlx fragment route splits: contents route first, releases generic
  second — FakeGet matches in order).
- Roadmap: DeepSeek-V4.1-Flash track item gets the sweep note + the
  revive condition (if the check ever flips, oQ2's 64% MMLU is the best
  measured ~2-bit 35B datapoint).

Suite: 505 green (1 pre-existing skip). Live run verified end to end:
new check reports "baseline recorded", snapshot updated.

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) is the critical path. The
vmlx_oq_loader check now watches the one condition that would revive the
oQ2 second candidate.

## 2026-09-22 ~19:25 PDT — mlxl3 battery adapter landed (measurement half of the mlxl3 wiring item)

Startup: tree clean. Circuit breaker clear (last two runs landed).

Watch sweep (`python3 -m tools.watch_sweep --full`): CHANGES (none) vs the
18:55 snapshot on all 11 checks — every avenue quiet (TQ ecosystem, JANGQ-AI /
Jundot / ddalcu uploads, Nemotron 0 dl, microsoft ternary, vmlx v1.6.64,
mlxl3 v1.1.1, 35B-A3B EXL3, novamlx v1.4.1, vmlx oQ loader still absent).

Substantive slice (mlxl3 wiring item, battery-adapter half): since the
EXL3 2.08bpw rung-4 candidate has no HTTP transport, the 19-prompt battery
is the rung verdict that matters per the standing direction — and it can
now drive it.

Landed:

- `tools/eval_battery_openai.py --transport mlxl3`: drives the mlxl3
  one-shot CLI via src/mlxl3_cli.py — one Mlxl3CliClient per --models
  entry (--max-tokens 512), --mlxl3-model/--mlxl3-bin/--mlxl3-timeout
  flags, shared _run_items loop (run_model signature/behavior unchanged),
  LLMError -> BatteryError mapping, <think> stripped as usual.
  UNTESTED-ON-MAC: real one-shot stdout shape is still the Mac-side
  validation in src/mlxl3_cli.py.
- `tools/measure_openai.py`: --transport mlxl3 is REFUSED loudly
  (exit 2, pointer to the battery) — the one-shot CLI exposes no token
  counts and per-invocation model-load cost is unmeasured, so tok/s would
  be meaningless. A fake speed number would corrupt the ladder.
- 11 new tests (fake-CLI script: scoring via real checks, think-strip,
  argv shape incl. --max-tokens 512, failure/timeout/missing-binary ->
  failed item, parse_args, end-to-end main() PASS + --mlxl3-model
  override), suite 516 green (1 pre-existing skip).

Self-reviewed diff: defaults unchanged (transport default http), no debug
prints, no secrets/paths, both module docstrings carry the Mac-side
invocation + the untested-on-Mac caveat.

Roadmap: mlxl3 wiring item marked partially landed; remaining (agent.py
`backend: mlxl3` opt-in) still contingent on the Mac-side stdout parse.

Next: unchanged — rung-4 Mac-side validation
(research/recipes/RUNG4_jang2s_35b_smelt.md) is the critical path; the
mlxl3 battery is ready the moment the Mac confirms the one-shot stdout
shape.

## 2026-09-22 ~19:50 PDT — rung-4 second-candidate recipe landed (EXL3/mlxl3)

Startup: tree clean. Circuit breaker clear (last runs all landed).

Watch sweep (`python3 -m tools.watch_sweep --full`): CHANGES (none) vs the
19:25 snapshot on all 11 checks — every avenue quiet.

Substantive slice (mlxl3 track): the yeasah 2.08bpw EXL3 weights were the
only rung-4 candidate with no documented Mac-side path — they had a
profile entry (status: watch) but no recipe. That gap is now closed.

Landed:

- `research/recipes/RUNG4_exl3_mlxl3.md`: the second rung-4 candidate
  (yeasah/Qwen3.6-35B-A3B-exl3 2.08bpw, author-reported +7.6% ppl vs
  bf16 at 2.13 bpw_layer — best ~2-bpw 35B-A3B datapoint on record)
  driven over the mlxl3 one-shot CLI bridge. Budget: 9.88 GiB
  author-reported resident (exllamav3, embeddings CPU-offloaded) + 0.04
  KV (f16 @ 2048, 3.6 arch) + ~1.0 runtime ≈ 10.9 GB — inside the ~11 GB
  budget but TIGHT; mlxl3's resident UNMEASURED, M1 Pro perf unmeasured
  (M5-only campaign). Honest caveats pinned in prose: author-reported
  figures, not-same-base (3.6 vs rung-3's 3.5), EXL3 skips embeddings,
  macOS 26.2+ + ad-hoc-signed-not-notarized Gatekeeper friction,
  measure_openai.py deliberately REFUSES --transport mlxl3 (no token
  counts, unmeasured per-invocation load cost), agent.py wiring waits on
  the Mac-side stdout-parse validation. Mac-side steps: install, hub
  download 2.08bpw revision (~11.72 GB disk), stdout-shape validation
  per src/mlxl3_cli.py's checklist, battery via
  `tools/eval_battery_openai.py --transport mlxl3`, scorecard vs rung 3
  (15/19 nominal, ~17/19 effective), resident-RAM measurement.
- `tests/test_rung_recipes.py`: TestRung4Exl3RecipeNumbers (KV 0.04,
  total ~10.92 < 11.0, roofline floor 20.2 > 10 usable bar, file exists)
  + TestRung4Exl3RecipeCaveats (candidate identity, all honesty caveats,
  speed-refusal note, wiring contingency — so a future edit can't
  silently drop them) + consistency-list entry.
- `research/model_profiles.yaml`: EXL3 profile notes reference the
  recipe (the consistency test's regex coupling now covers it).
- Roadmap: mlxl3 track item notes the recipe; JANG_2S stays the primary
  rung-4 candidate (same-base 3.5, real HTTP transport, measurable today).

Suite: 524 green (1 pre-existing skip). One self-caught failure during
the run: a new test pinned the word "contingent" but the recipe says
"wiring waits on the Mac-side stdout-parse validation" — test fixed to
pin the recipe's actual wording, suite re-ran green. Self-reviewed diff:
no debug prints, defaults untouched, no secrets/paths.

Commits 5187bb4 + a6f1d32; both mirrored to GitHub and verified (GitHub
main's commit messages match local).

Next: unchanged — rung-4 Mac-side validation is the critical path: the
JANG_2S recipe (primary) and now the EXL3/mlxl3 recipe (second
candidate, gated on the mlxl3 stdout-shape check in src/mlxl3_cli.py).
