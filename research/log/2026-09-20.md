# Research log — 2026-09-20 (initial sweep)

Mission kickoff: find a path to run a 70B-class model on Justin's 16GB
M1 Pro Mac. Budget reality check: macOS takes ~4–6GB, leaving ~10–11GB
for weights + KV cache + runtime.

## Findings

1. **llama.cpp now has ternary quants.** TQ1_0/TQ2_0 (1.7–2.1 bits/param)
   and IQ1_S/IQ1_M (1.5–1.75 bpw) exist in the quant zoo. A 70B at TQ1_0
   is ~14.9GB on disk — the smallest a dense 70B GGUF has ever been.
   Open: whether Ollama's bundled llama.cpp supports TQ yet, and whether
   70B TQ1_0 GGUFs exist on HuggingFace.
   Source: https://blog.csdn.net/gitblog_00939/article/details/150958360

2. **Native ternary 70B would be ~13.8GB** (16/1.58 ≈ 10.1x smaller than
   FP16's 140GB) — fits a MacBook Pro in principle, and ternary ops are
   add/sub instead of multiplies, so CPU inference is fast.
   Source: https://github.com/broomva/broomva.tech/blob/HEAD/apps/broomva/content/writing/bitnet-1bit-llms-agentic-edge.mdx

3. **But no good 70B ternary model exists.** Public ternary models top out
   ~8–10B; the Llama3-8B-1.58 conversion (100B tokens) is reported poor
   quality; no 1T-token 70B ternary model is announced as of Sep 2026.
   This is the #1 thing to watch — a proper release changes everything.
   Source: https://github.com/toastmanau/wyltek-industries/blob/HEAD/research/bitnet-well-trained-models-2026.md

4. **Mixed-precision 2-bit MoE is the practical path today.** oQ (oMLX)
   does data-driven per-layer bit allocation: Qwen3.5-35B-A3B at 2-bit
   scores 64% MMLU vs 14% for naive mlx-lm 2-bit; JANG does similar
   adaptive allocation (attention/rounters at 6–8bit, experts at 2–4bit).
   A 35B MoE at effective 2-bit ≈ 8.8GB — fits with room to spare, and
   MoE's few active params decode fast on bandwidth-bound Apple Silicon.
   Sources: https://github.com/jundot/omlx/blob/HEAD/docs/oQ_Quantization.md
   and https://github.com/imfantuan/jangq-mlx

5. **KV-cache compression frees headroom.** VeloxQuant-MLX / TurboQuant
   claim 5–8x KV cache reduction at near-fp16 quality; Ollama has
   `OLLAMA_KV_CACHE_TYPE=q8_0|q4_0` env. Small for 70B (~1.3GB at 4k ctx)
   but every GB counts against the ~10–11GB budget.
   Sources: https://github.com/rajveer43/veloxquant-mlx/blob/HEAD/docs-site/blog/2026-05-28-hands-on.md
   and https://medium.com/@kapildevkhatik2/optimizing-ollama-performance-on-windows-hardware-quantization-parallelism-more-fac04802288e

6. **70B at Q4 is ~40GB** — the "everyone knows" number, confirming dense
   70B at standard quants is ~4x over budget.
   Source: https://github.com/prime-basin-ai/ai-software/blob/HEAD/research/llm-runners/llama.cpp.md

## Actions taken
- Created `research/ROADMAP_70B.md`, `research/model_profiles.yaml`.
- Initialized git in `~/workspace/local-agent`; initial build committed as
  `5faaf30`. All future changes will be commits (revertible).
- Set up weekly Monday-morning research sweep (cron) to keep tracking.

## Next sweep should check
- Ollama release notes: TQ1_0/TQ2_0 support landed?
- HuggingFace: any 70B IQ1_M / TQ1_0 GGUFs with quality reports?
- microsoft/BitNet + bitnet.cpp: 70B ternary model news?
- oQ/JANG: new releases, more 2-bit MoE quality data?

## Engineering session — 2026-09-20 ~20:45 PDT (commit 8b81aba)

Picked up the top unchecked backlog item: **Quant R&D prototypes (avenue H)**.
Scaffolded the experiment harness as `src/quant_rnd/` — first slice only,
tested partial commit, next step recorded below.

**Landed:**
- `src/quant_rnd/schemes.py`: 3 baselines + 2 candidate schemes, all
  group-wise (128), with honest bpw accounting (payload + scale/index
  overhead per group):
  - baselines: `ternary_uniform` (T1_0-like, absmean scale, 1.71 bpw),
    `int2_symmetric` (naive 2-bit, 2.13 bpw),
    `int2_outlier_retain` (2-bit + 0.5% fp16 outliers, mixed-precision
    flavor of the oQ/JANG idea, 2.24 bpw)
  - candidates (ours): `dual_scale_ternary` (asymmetric ternary — separate
    +/- scales for skewed tensors, 1.84 bpw), `ternary_outlier` (ternary +
    2 exact fp16 outliers/group, 2.06 bpw)
- `src/quant_rnd/bench.py`: synthetic-weight SQNR benchmark
  (`python3 -m src.quant_rnd.bench`); weights mimic LLM stats (Gaussian
  body + 0.1% 8x outliers).
- `tests/test_quant_rnd.py`: 12 tests; full suite 26 green.

**First synthetic results (PROXY ONLY — not real-model perplexity):**

| scheme | SQNR (dB) | bpw |
|---|---|---|
| ternary_outlier (candidate B) | 6.73 | 2.060 |
| int2_outlier_retain (baseline) | 6.46 | 2.235 |
| dual_scale_ternary (candidate A) | 5.59 | 1.835 |
| ternary_uniform (baseline) | 5.56 | 1.710 |
| int2_symmetric (naive baseline) | 2.51 | 2.125 |

Read: naive symmetric int2 is destroyed by outliers (expected — this is why
oQ/JANG's adaptive allocation matters); ternary + sparse exact outliers beats
it at matched bitrate on synthetic data; dual-scale ternary clearly beats
symmetric ternary on skewed tensors (test asserts >1 dB margin). Nothing here
is a claim about real 70B quality yet.

**Backlog grown** (roadmap): real-tiny-model perplexity validation, Pareto
sweep of outlier params, ggml kernel design if a candidate holds up, and a
K-means 2-bit baseline (the fair classical comparison the naive int2 lacks).

**Next session:** wire the bench to a real 60–130M param model for actual
perplexity vs the synthetic ranking; then the Pareto sweep.

**Ops notes:** `git status` was clean at start; default `backend: ollama` +
`qwen3:8b` untouched (new files only). GitHub mirror pushed after one
transient RemoteDisconnected retry (pushed commit aea923c44673). Set
repo-local git identity (Hearth <hearth@local>) to match prior commits —
the VM had lost it.

## Engineering session — 2026-09-20 ~21:15 PDT

**Startup:** `git status` showed the prior session's log entry uncommitted
(log-only, no code). Committed it alone first (ba47067) so the tree was
clean before starting; tests were green throughout.

**Picked item:** Quant R&D K-means (Lloyd) 2-bit baseline — the "fair
classical comparison the naive int2 lacks." Landed as `int2_kmeans` in
`src/quant_rnd/schemes.py`: per-group 1-D Lloyd (deterministic quantile
init, 20 iters max), 4 fitted fp16 centroids/group, codes index the
centroids. Honest bpw: 2.5 (2-bit payload + 4×fp16/128). 6 new tests;
full suite 32/32 green.

**Synthetic results (default bench, seed 7, 64 groups):**

| scheme | SQNR (dB) | bpw |
|---|---|---|
| int2_kmeans (new classical baseline) | 9.68 | 2.500 |
| ternary_outlier (candidate B) | 6.73 | 2.060 |
| int2_outlier_retain | 6.46 | 2.235 |
| dual_scale_ternary (candidate A) | 5.59 | 1.835 |
| ternary_uniform | 5.56 | 1.710 |
| int2_symmetric (naive) | 2.51 | 2.125 |

**Honest read (negative for our candidates, recorded as such):** the
classical baseline beats both candidates on synthetic SQNR — but at the
highest bitrate (2.5 bpw). Our candidates remain the cheapest-per-dB
options below 2.1 bpw. Bitrates differ, so this is not yet a matched
comparison. **Backlog grown:** add a matched-bitrate k-means variant
(8-bit centroids -> ~2.25 bpw) to see if ternary_outlier still loses at
equal bitrate; Pareto sweep item now covers 6 schemes.

**Not attempted this session:** real tiny-model perplexity (needs
torch/transformers — not installed on this VM; install + ~500MB model
download exceeds a 25-min slot). Next session could start there with a
pip install first, or continue the synthetic Pareto sweep.

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched (new
scheme opt-in via SCHEMES registry only). No secrets. Docs (docstrings,
roadmap) updated with the new result.

## Engineering session — 2026-09-20 ~21:45 PDT (commit 7eba663)

**Startup:** `git status` clean; tests green at start.

**Picked item:** Quant R&D matched-bitrate k-means variant — the follow-up
the last session left: does ternary_outlier still lose to the classical
baseline at equal bitrate?

**Landed:** `int2_kmeans_q8` in `src/quant_rnd/schemes.py` — same per-group
Lloyd 2-bit fit, but the 4 centroids are stored in 8-bit (symmetric, one
fp16 codebook scale per group), the trick real IQ codebook quants use.
bpw = 2.0 + 4x8/128 + 16/128 = 2.375 (vs 2.5 for the fp16-codebook variant).
`reconstruct()` measures exactly what a decoder sees: scales hold the
dequantized (8-bit-rounded) centroids, and code assignment is done against
the rounded codebook. 7 new tests (bpw math, determinism, codebook-lookup
reconstruction, rounding-cost bound, the matched-bitrate verdict,
constant-input edge case); full suite 39/39 green.

**Synthetic results (default bench, seed 7, 64 groups):**

| scheme | SQNR (dB) | bpw |
|---|---|---|
| int2_kmeans | 9.68 | 2.500 |
| int2_kmeans_q8 (new) | 9.68 | 2.375 |
| ternary_outlier (candidate B) | 6.73 | 2.060 |
| int2_outlier_retain | 6.46 | 2.235 |
| dual_scale_ternary (candidate A) | 5.59 | 1.835 |
| ternary_uniform | 5.56 | 1.710 |
| int2_symmetric (naive) | 2.51 | 2.125 |

**Two findings, both worth keeping:**
1. 8-bit codebook rounding is essentially free: 9.0486 vs 9.0489 dB on
   the test tensor. Useful for the future ggml-kernel item — an IQ-style
   8-bit codebook costs nothing in fidelity.
2. **Negative result (recorded honestly):** ternary_outlier still loses to
   Lloyd k-means by >2 dB even at near-matched bitrate (6.60 @ 2.06 vs
   9.05 @ 2.375). On synthetic SQNR, classical Lloyd owns the ~2.4 bpw
   Pareto point. The candidates' case now has to rest on ternary compute
   (add/sub vs multiplies), not fidelity — backlog grown with a pivot
   item, and a note to check the ranking (not absolute dB) on real-model
   perplexity when that run happens.

**Not attempted:** real tiny-model perplexity (still needs torch install +
model download; noted as a bigger slot).

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched (new scheme
opt-in via SCHEMES registry only). No secrets. GitHub mirror pushed
(2e644ad40031).

## Engineering session — 2026-09-20 ~22:15 PDT (commit 96fa072)

**Startup:** `git status` clean; full suite 39/39 green at start. No
circuit-breaker trip (last two runs both landed).

**Picked item:** Quant R&D Pareto sweep (outlier_frac / n_outliers /
group sizes) — the experiment the pivot item needed to answer part (a):
can any scheme beat Lloyd k-means below ~2.2 bpw on SQNR?

**Landed:**
- `src/quant_rnd/sweep.py`: `python3 -m src.quant_rnd.sweep` sweeps
  ternary_outlier n∈{1,2,4,8}, int2_outlier_retain frac∈{0.001..0.02},
  int2_kmeans/q8 group_size∈{64,128,256}, plus DST and symmetric ternary
  as single points; marks the Pareto frontier (best SQNR at/below each
  bitrate). 7 new tests (46/46 green), incl. a sweep/bench consistency
  check (n=2 point agrees with the unswept bench to 1e-9 bpw).
- **Bug fix the sweep exposed (real, not cosmetic):**
  `QuantResult.reconstruct()` hard-coded GROUP_SIZE=128, so any
  non-default group size crashed with IndexError. `group_size` is now
  stored on the result; all quantizers pass it through.

**Sweep results (seed 7, 64 groups; skewed run with mean shift 0.5):**

| config | SQNR (dB) | bpw | frontier? |
|---|---|---|---|
| ternary_uniform | 5.56 | 1.710 | * |
| dual_scale_ternary | 5.59 | 1.835 | * |
| ternary_outlier n=1 | 6.41 | 1.885 | * |
| ternary_outlier n=2 | 6.73 | 2.060 | * |
| int2_kmeans_q8 g=256 | **9.12** | **2.188** | * |
| int2_kmeans g=256 | 9.12 | 2.250 | * |
| int2_kmeans_q8 g=128 | 9.68 | 2.375 | * |
| int2_kmeans g=128 | 9.68 | 2.500 | * |
| int2_kmeans_q8 g=64 | 10.09 | 2.750 | * |
| int2_kmeans g=64 | 10.09 | 3.000 | * |

**Verdict (negative for our candidates, recorded honestly):** pivot
part (a) is ANSWERED NO — Lloyd owns every bitrate ≥2.19 bpw, on clean
*and* skewed tensors (Lloyd's SQNR is skew-invariant: 9.12/9.68/10.09 dB
in both runs). Ternary family only owns the frontier below ~2.06 bpw.
No amount of outlier-tuning closes the ~2.4 dB gap at matched bitrate.
**The fidelity path for ternary schemes is exhausted on synthetic data.**
Part (b) is now the main thread: the case for ternary must rest on
inference compute cost (add/sub vs multiplies), not SQNR.

**Backlog grown (roadmap):** (1) candidate C — "Lloyd-fit ternary"
(1-D Lloyd with codebook constrained to ternary {-s,0,+s} or dual-scale
ternary; tests whether k-means *fitting* is the real ingredient, at
1.71–1.84 bpw); (2) adopt `int2_kmeans_q8` at group 256 (2.188 bpw) as
the reference baseline-to-beat; (3) op-count model of ternary matmul vs
int2 codebook dequant-multiply for the Apple-Silicon decode regime.

**Not attempted:** real tiny-model perplexity (still needs torch install
+ model download; a bigger slot).

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched. No
secrets. GitHub mirror pushed (9f1ca96a766e).

## Engineering session — 2026-09-20 ~22:45 PDT (commit ca1c1e0)

**Startup:** `git status` clean; full suite 46/46 green at start. No
circuit-breaker trip (last two runs both landed).

**Picked item:** pivot part (b) — the op-count model of ternary matmul vs
int2 codebook dequant for the Apple Silicon decode regime. This was the
explicitly-designated main thread after the Pareto sweep killed the
fidelity path.

**Landed:** `src/quant_rnd/opcount.py` — two-layer quantitative model:
(1) decode roofline (tok/s ~= bandwidth / bytes_per_token; bpw sets the
ceiling); (2) per-weight op counts (ternary: signed add or zero-skip +
1 scale mul/group; codebook: naive 1 mul+1 add/weight vs histogram trick
1 add/weight + few ops/group) + energy proxy (mul ~= 3.5 adds).
Exported lazily from `src/quant_rnd/__init__.py` like the bench names.
11 new tests (57/57 green), incl. an end-to-end test that runs the real
quantizers and asserts ternary wins on BOTH roofline and op cost, and a
KV-cache test pinning the roadmap's ~1.3 GB figure (1.342 GB computed).

**Numbers (70B scale, 200 GB/s, 1.34 GB KV @ 4k fp16, seed 7, g=128):**

| scheme | bpw | weight GB | t/s ceiling | equiv-adds/w |
|---|---|---|---|---|
| ternary_uniform | 1.710 | 14.96 | 12.3 | 0.71 |
| dual_scale_ternary | 1.835 | 16.06 | 11.5 | 0.74 |
| ternary_outlier n=2 | 2.060 | 18.03 | 10.3 | 0.79 |
| int2_kmeans_q8 (histogram) | 2.375 | 20.78 | 9.0 | 2.17 |
| int2_kmeans (histogram) | 2.500 | 21.88 | 8.6 | 2.17 |

**Headline:** ternary_uniform gets a **1.36x higher decode ceiling and
3.0x lower energy-proxy op cost** than the Lloyd reference
int2_kmeans_q8. All schemes are bandwidth-bound (2.8-7.7 FLOP/byte vs
~10 machine balance), so the bpw-driven byte reduction is the
first-order effect; the add/sub MAC advantage is second-order on
decode, first-order in compute-bound regimes (prefill). Honest caveats
kept in the module docstring and roadmap: MODEL ONLY — ceilings, not
measured tok/s; 14.96 GB still exceeds the ~10-11 GB usable budget, so
this is the *speed* case contingent on a fitting path.

**Backlog grown (roadmap):** (1) Mac-side validation of the 1.3-1.4x
roofline ratio with real tok/s measurements (needs Justin's Mac);
(2) prefill (compute-bound) roofline extension to opcount.py, where the
3x equiv-adds advantage becomes first-order.

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched. No
secrets. GitHub mirror pushed (42b26a922741).
