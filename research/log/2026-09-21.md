# Engineering log 2026-09-21

## Engineering session 2026-09-21 ~00:15 PDT — 1-step Lloyd + opcount re-run

**Picked:** unchecked backlog item "1-step Lloyd ternary scheme" (follow-up
from the Lloyd-gain diagnostic) + the "re-run opcount with the fitted
reference" item, since the first's zero-rate numbers feed the second.

**Landed:** commits `eebf806`, `02d6ca0`, `b041d58` (roadmap). Suite
81/81 green.

**Candidate D — `ternary_1step` (`src/quant_rnd/schemes.py`):** one Lloyd
iteration — heuristic thresholds (+/-absmean/2) -> one L2-optimal scale
refit -> reassignment at the refit thresholds — via
`_ternary_lloyd_fit(n_iter=1)`. O(1) extra cost (two passes/group, no
convergence loop), identical 1.710 bpw to ternary_uniform.

**Numbers (seeds 7–9, clean + skew 0.5, 64 groups):** capture of the full
ternary_lloyd win over ternary_uniform = 0.84–0.87 (seed 7 clean: 6.72 dB
vs uniform 5.56 / full Lloyd 6.93). The hypothesis HOLDS — one iteration
is the practical encoder for fitted ternary. Honest negative: a stricter
"refit scale, keep heuristic codes" variant was tried and scores 6.45 dB
— decoding must reassign at the refit thresholds.

**Opcount re-run with the fitted reference** (`test_fitted_ternary_opcount_reference`):
decode ceiling ratio UNCHANGED at 1.357x (roofline is byte-driven,
sparsity-independent in this model); energy-proxy ratio RISES 3.04x ->
3.52x vs int2_kmeans_q8 because the fitted encoder's measured zero-rate is
0.41 vs ternary_uniform's 0.31 (fewer adds/weight). The ternary compute
case is slightly stronger than first reported.

**Backlog grown:** "1-step Lloyd" dual-scale variant for skewed tensors
(check capture vs ternary_lloyd_ds).

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched. No
secrets. GitHub mirror pushed (170758c6b478).

## Engineering session 2026-09-21 ~00:45 PDT — dual-scale "1-step Lloyd"

**Picked:** unchecked backlog item "'1-step Lloyd' dual-scale variant"
(the symmetric twin captured 0.84–0.87 in the previous session; this
asks whether one fit iteration also captures the dual-scale win on
skewed tensors).

**Landed:** commit `7eae088` (+ roadmap `...`). Suite 88/88 green (+7).

**Candidate D-dual — `ternary_1step_ds` (`src/quant_rnd/schemes.py`):**
`_ternary_lloyd_fit` with dual=True, n_iter=1 — heuristic dual thresholds
(conditional means per side) -> one per-side L2-optimal refit ->
reassignment. Same 1.835 bpw at group 128 as ternary_lloyd_ds
(registered in _DUAL_SCALE_SCHEMES so reconstruct() uses both scales).
n_iter exposed for the convergence check.

**Numbers (seeds 7–9, clean + skew 0.5, 64 groups, capture of
ternary_lloyd_ds's win over dual_scale_ternary):**

| seed | skew | n=1 | n=2 | n=3 |
|---|---|---|---|---|
| 7 | 0.0 | 0.83 | 0.96 | 0.98 |
| 7 | 0.5 | 0.73 | 0.84 | 0.87 |
| 8 | 0.0 | 0.85 | 0.97 | 0.99 |
| 8 | 0.5 | 0.78 | 0.89 | 0.90 |
| 9 | 0.0 | 0.55 | 0.63 | 0.65 |
| 9 | 0.5 | 0.57 | 0.67 | 0.76 |

**Verdict: PARTIAL.** One iteration does NOT replicate the symmetric
twin's 0.84–0.87 capture; the dual case converges slower. 2–3 fixed
iterations (still O(1), no convergence loop) get 0.63–0.99 — so the
practical dual-scale encoder is 2–3 iterations, not 1. Seed 9 skew 0.5
is the slowest (needs n=5 for full convergence) — a slow-convergence
case worth understanding when adaptive stopping is designed.

**Scratch-methodology note:** my first scratch measurement ran the dual
scheme under an unregistered QuantResult name and reconstruct() fell
back to single-scale decoding, underestimating the dual 1-step by
~0.1 capture. The landed numbers use the registered name. Lesson: the
_DUAL_SCALE_SCHEMES registration is load-bearing for correctness —
worth a comment, which is now in the scheme docstring.

**Backlog grown:** opcount energy/speed re-run with the dual fitted
reference (ternary_1step_ds n=2, per-side zero-rates).

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched (suite
green). No secrets. No model downloads.

## Engineering session 2026-09-21 ~01:15 PDT — dual fitted opcount re-run

**Picked:** unchecked backlog item "re-run the opcount energy/speed
comparison with the dual fitted reference (`ternary_1step_ds` at
n_iter=2, measured zero-rate per side)" — the direct continuation of the
previous session's dual 1-step result.

**Landed:** commit `f02df67` (+ GitHub mirror 653d455a78e9). Suite 90/90
green (+2 tests).

**`side_fractions()`** (`src/quant_rnd/opcount.py`): per-code fractions
{pos, neg, zero} for ternary-family QuantResults; raises on non-ternary
codes instead of silently mis-measuring. Rationale: a dual-scale sparse
kernel accumulates the two nonzero sides under separate scales, so the
honest op input is the per-side profile, not just total sparsity.

**`test_dual_fitted_ternary_opcount_reference`** (seed 7, 64 groups,
g128, n_iter=2): decode ceiling ratio 1.272x vs int2_kmeans_q8 (lower
than symmetric 1-step's 1.357x — byte-driven, dual carries 1.835 vs 1.710
bpw); energy-proxy ratio 3.53x clean / 3.40x skewed (vs symmetric's
3.52x). Dual zero-rates: 0.441 clean, 0.418 skew 0.5. Per-side split on
the skewed tensor: pos=0.415 / neg=0.168 — the asymmetric op profile the
re-run was meant to capture; a test pins pos > 2x neg. The dual's extra
scale multiply (n_scales=2) costs ~0.016 muls/weight — genuinely slight.
Bottom line: the ternary compute case holds with the dual fitted
reference; the numbers barely move, as predicted.

**Backlog grown:** "when the tiny-model perplexity run selects a real
group size (128 vs 256), re-run the opcount table at that group size"
(g256 dilutes the dual decode ratio to 1.177x — measured in scratch).

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched. No
secrets. No model downloads.

## Engineering session 2026-09-21 ~01:45 PDT — prefill roofline

**Picked:** unchecked backlog item "extend opcount.py with a prefill
(compute-bound) roofline" — the regime where the roadmap predicted
ternary's op advantage becomes first-order. Direct continuation of the
opcount thread.

**Landed:** commit `7ff4fd9` (+ GitHub mirror 2bfd47c74f80). Suite 91/91
green (+1 test).

**`prefill_roofline_tps()`** (`src/quant_rnd/opcount.py`): prompt-level
roofline — weights streamed once, activations at an estimated
6*d_model elems/token/layer, KV write for the prompt, FLOPs counted as
adds+muls per weight (lookups aren't FLOPs). peak_flops is an explicit
argument (no baked-in constants); tests use Apple-published 5.2 TFLOPS
FP32 M1 Pro GPU. Honest caveats in the docstring: adds count as 1 FLOP
against an FMA-counted peak (conservative for ternary — up to ~2x
headroom on FMA hardware, stated not folded in); attention O(L^2)
ignored (<5% at 4k, known under-count at long ctx).

**Result:** the regime flip is real. L=1: both references bandwidth-bound
(AI 2.8 vs balance 26 FLOP/byte). L=512+: both compute-bound (AI 100x+
balance) and the ceiling ratio is EXACTLY the FLOP-per-weight ratio:
**1.795x ternary_1step over int2_kmeans_q8** (peak, efficiency, and
prompt length all cancel in the test). Note: this is the FLOP-counted
ceiling ratio; the 3.5x energy-proxy ratio is a different (softer)
number — reported separately, not conflated.

**Backlog grown:** Mac-side validation of the prefill ceiling ratio
(needs Justin's Mac); attention O(L^2) term for long context (32k+).

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched (suite
green). No secrets. No model downloads.

## Engineering session 2026-09-21 ~02:15 PDT — real-weight SQNR probe

**Picked:** unchecked backlog item "check whether the synthetic SQNR
ranking reproduces on real weights" — the highest-priority R&D item
runnable from this VM. Downloaded GPT-2 124M fp32 (524 MB, single
safetensors, NOT multi-GB so allowed) to research/data/ (gitignored,
reusable by future runs). /tmp is a 512 MB tmpfs — the download OOM'd
there first; workspace disk holds it fine.

**Landed:** commit `fac1f26` (+ GitHub mirror pending). Suite 97/97
green (+6 tests).

**`src/quant_rnd/realweights.py`:** dependency-free .safetensors parser
(NumPy only — header struct + JSON, F32/F16 supported, BF16 refused
explicitly), linear-weight-matrix selection (2-D `.weight`, embeddings
excluded by name, documented), seeded group-aligned block sampling,
`rank_on_real_weights` (mean SQNR per scheme over 48 matrices, 64
groups/matrix). CLI: `python3 -m src.quant_rnd.realweights
research/data/gpt2.safetensors`.

**Result — ANSWERED YES, the ranking transfers.** Real weights (seeds
7+8) vs synthetic seed-7: Lloyd k-means 9.4–9.5 (9.68) >>
ternary_lloyd_ds 7.0–7.1 (7.00) > ternary_lloyd 6.8–6.9 (6.93) >
ternary_1step_ds ~= ternary_outlier 6.5–6.6 (6.77/6.73, tied within
noise) > ternary_1step 6.4–6.5 (6.72) > int2_outlier_retain 5.8–5.9
(6.46) > dual_scale_ternary 5.3–5.4 (5.59) > ternary_uniform 5.25–5.33
(5.56) > int2_symmetric 2.8–3.1 (2.51). Absolute dB within ~0.3 dB of
synthetic for all but int2_outlier_retain (-0.6 dB: real outliers harder
to isolate than planted ones). The fitted-ternary story (1-step ~85%
capture at 1.710 bpw, dual 2–3 iters) survives real distributions.
Remaining caveat is perplexity, not distribution.

**Backlog grown:** numpy GPT-2 perplexity harness (parser + weights
already in place); group-size sensitivity of the real-weight ranking
(g64/g256 feeds the opcount table choice); embedding-table quantization
probe.

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched (suite
green). No secrets. No multi-GB downloads.

**Mirror note (02:30 PDT):** the gh_push.py run for `fac1f26` exited 0 but
printed only HTML fragments instead of its usual "collected N files /
pushed commit" lines, so the mirror is UNCONFIRMED. The public API can't
see the private repo to verify. Local git (commits fac1f26 + e277f3e) is
the source of truth; the next run can re-mirror if the skill reports a
clean push. No retry this run — no point pushing a redundant mirror
commit on ambiguous evidence.

## Engineering session 2026-09-21 ~02:45 PDT — real-weight group-size sensitivity

**Picked:** unchecked backlog item "group-size sensitivity of the real-weight
ranking — re-run realweights.py at g64 and g256". The CLI didn't expose
`--group-size`, so first: factored `parse_args()` out of `main()` and added
the flag (default = GROUP_SIZE, behavior unchanged).

**Landed:** (pending commit) 99/99 tests green (+2 tests).

**Result — ranking is group-size invariant.** GPT-2 124M, 48 matrices, 64
groups/matrix, seed 7. Lloyd k-means first, then ternary_lloyd_ds, then
ternary_lloyd at all three sizes:

| scheme | g64 | g128 | g256 |
|---|---|---|---|
| int2_kmeans_q8 (classical ref) | 9.94 @ 2.750 | ~9.4 @ 2.375 | 9.09 @ 2.188 |
| ternary_lloyd_ds | 7.34 @ 2.085 | ~7.0 @ 1.835 | 6.83 @ 1.710 |
| ternary_lloyd | 7.07 @ 1.835 | ~6.8 @ 1.710 | 6.72 @ 1.647 |

All schemes lose SQNR as groups grow (Lloyd 9.95→9.09; naive
int2_symmetric collapses at g256: 1.21 dB). One expected shuffle:
ternary_outlier passes ternary_lloyd at g64 (7.21 vs 7.07 @ 2.535 bpw) and
falls below ternary_1step at g256 (6.21 vs 6.30 @ 1.823 bpw) — its bitrate
scales with n_outliers/group, so this is bookkeeping, not a story change.
The distribution caveat is now closed across group sizes; perplexity is
still the open caveat.

**Backlog grown:** opcount-table item now notes the ranking doesn't prefer a
group size — the choice rests on the pending perplexity run + bpw budget
(g64 is SQNR-best but bpw-worst).

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched (suite
green). No secrets. No downloads (reused research/data/gpt2.safetensors).

**Mirror note (03:00 PDT): MIRROR BEHIND.** The gh_push.py run for `5c6e18e`
died on a VM network SSL error ("EOF occurred in violation of protocol");
a single retry after 10s failed the same way. GitHub main still shows the
older "prefill (compute-bound) roofline" message, so local commits
`fac1f26`, `e277f3e`, `242f046`, and `5c6e18e` are NOT mirrored yet. Local
git is the source of truth; a later session's push will catch up.

## 03:00 PDT — mirror fixed (main agent)
- Root cause of the 02:15–02:45 mirror failures: gh_push.py walked the working tree WITHOUT respecting .gitignore, so it tried to upload research/data/ (523 MB of GPT-2 weights) as GitHub blobs; the huge upload died with SSL EOF every time. Small API calls kept working, which made it look like transient network flakiness.
- Fix: gh_push.py now parses the root .gitignore (supports *, ?, [...], trailing / for dirs, / anchoring, ! negation) and prunes ignored paths before collecting. Verified: 38 files / 0.2 MB collected, research/data/ and *.pyc excluded.
- Mirror caught up manually: pushed commit 33bb47fc760a to VihaanGopee/hearth@main containing the full tree through local HEAD (message differs from local HEAD by design on catch-up pushes; content is complete). Next normal session push will resume message parity.
- Lesson: the loop's new verify-the-mirror rule (added ~02:40) stays — it caught this. Skill doc updated.

## Engineering session 2026-09-21 ~03:45 PDT — perplexity harness slice 1 (forward + tokenizer + fp32 reference)

**Picked:** unchecked backlog item "validate candidates on a real tiny
model — real perplexity vs the synthetic SQNR ranking". Split it: this
session lands the harness + fp32 reference (step 2a); per-scheme
quantized perplexity (step 2b) is the exact next step, noted in the
roadmap.

**Landed:** commit `48dd13e` — 116/116 tests green (+17).

- `src/quant_rnd/gpt2_tokenizer.py`: dependency-free byte-level BPE
  (HF GPT2Tokenizer convention). vocab.json + merges.txt downloaded
  from HF (~1.5 MB, allowed — not multi-GB). Pre-tokenizer uses an
  ASCII-safe rendering of the GPT-2 regex (exact on ASCII input, noted
  in the docstring); byte-level fallback keeps non-ASCII round-tripping.
- `src/quant_rnd/gpt2_forward.py`: NumPy GPT-2 124M forward (pre-norm,
  causal attention, exact-erf GELU, lm_head tied to wte) +
  `perplexity()`. GELU uses a vectorized Abramowitz-Stegun erf
  (max err 1.4e-7 vs math.erf, tested) — the scalar erf was NOT the
  bottleneck; this VM has no optimized BLAS, so the logits matmul
  dominates (~40 s per 406-token forward).
- `research/data/eval_text.txt`: hand-composed ASCII prose, 406 tokens.
- **fp32 reference perplexity: 53.50** — sane band for GPT-2 124M on
  literary prose, verifying the forward pass end to end (a broken arch
  would give thousands; cheating/no-mask would give <10).
- `.gitignore` refined: `research/data/*` ignored except `eval_text.txt`
  and `tokenizer/` — harness reproducible without the 523 MB weights.

**Backlog grown:** roadmap item now records the 2a/2b split and the
53.50 reference; added an item on the no-BLAS ~40 s/forward cost with
mitigations for the step-2b sweep.

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched (suite
green). No secrets. No multi-GB downloads.

**Mirror:** gh_push.py printed clean "collected 43 files / pushed commit
4263b9d92ef3" and GitHub main's commit message matches local HEAD
(`48dd13e` message). Mirror current.

## 04:45 PDT — session timed out, WIP stashed (main agent)
- The 04:15 session attempted the per-scheme quantized-perplexity slice (ppl.py + quantize_model + gated e2e tests) but both the unittest suite (20+ min) and the ppl sweep (18+ min) were still running with no output at timeout. Nothing was committed; no log entry or mirror happened.
- Suspected cause: the new gated end-to-end test class runs real-weight quantization plus a 16-token forward pass per scheme under CPU contention — likely just slow, possibly stuck. Needs investigation, not blind re-run.
- Recovery: stashed the uncommitted work as "WIP: per-scheme quantized perplexity (ppl.py) - uncommitted, suite timed out 2026-09-21 04:40 PDT" (`git stash list`). Syntax-checked OK (py_compile). Tree is clean.
- Next session: `git stash pop`, profile the slow test/sweep first (run the gated e2e test alone with a timeout to see if it finishes), then run the full suite, self-review, commit, mirror. If the sweep completed numbers exist nowhere yet — the per-scheme bpw-vs-ppl table vs the 53.50 fp32 reference is still open.

## Engineering session 2026-09-21 ~04:45 PDT — recovered the 04:15 WIP, found + fixed the perf bug that killed it

**Picked:** the stashed WIP from the timed-out 04:15 session
(per-scheme quantized perplexity: ppl.py + quantize_model + gated e2e
tests), per the recovery plan in the 04:45 log note.

**Root cause of the 04:15 timeout — FOUND.** Not CPU contention:
`QuantResult.reconstruct()` in src/quant_rnd/schemes.py was an O(n^2 /
group_size) per-group boolean-mask loop (`mask = group_id == gi` scans
the whole flat array per group). Measured: 25.3 s per 2M params =>
~18 min per full 85M-param model PER SCHEME. The 04:15 sweep sat in
reconstruct with no output for 18+ min; the gated e2e test hung the same
way. (The `| tail -12` pipe in the old run also hid streaming output —
a red herring on top.)

**Landed:** commit `4b5c6cc` — 127/127 tests green (+11).

- `src/quant_rnd/ppl.py` (new): step 2b harness — `quantize_model`
  (quantize + reconstruct every linear weight group-wise, fp32
  pass-through for embeddings/biases/LN), `run_sweep`, CLI with
  `--schemes`/`--group-size`. Lloyd k-means family deliberately excluded
  from DEFAULT_SWEEP (too slow: ~7 min/full-model); runs as follow-up.
- `reconstruct()` vectorized to O(n) via group fancy indexing:
  25.3 s -> 25 ms per 2M params (~1000x), values bit-identical across
  all decode branches (single-scale, dual-scale, codebook, outliers).
- Gated e2e test now passes in 37 s (was: indefinite hang): real GPT-2
  124M quantized with ternary_1step forwards 16 tokens with finite
  logits.
- New tests: TestQuantizeModel (7, plumbing), TestReconstructVectorized
  (2: pins vectorized reconstruct to the old per-group semantics incl.
  trailing partial group + a model-scale <10 s runtime guard),
  TestQuantizedPerplexityEndToEnd (2, gated on the weights file).

**Result:** the step-2b sweep (per-scheme ppl vs the 53.50 fp32
reference) is now FEASIBLE — it was impossible before this fix. Not run
yet this session (time); it is the exact next step:
`python3 -m src.quant_rnd.ppl research/data/gpt2.safetensors`
(~10-12 min for the 7-scheme default sweep on this VM).

**Backlog grown:** roadmap item "vectorize the ternary Lloyd-fit
encoder" (~2 Mparams/s pure-Python loop is now the dominant per-scheme
cost: ternary_1step ≈ 45 s, ternary_lloyd n_iter=20 ≈ 2 min full-model).

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched (suite
green). No secrets. No downloads. Roadmap change committed with the
code (single commit `4b5c6cc`).

**Mirror:** gh_push.py clean ("collected 44 files / pushed commit
079637b1762c"); GitHub main's commit message matches local HEAD.
Mirror current.

## Engineering session 2026-09-21 ~05:15 PDT — per-scheme quantized perplexity sweep (step 2b) LANDED

**Picked:** the exact next step from the 04:45 session — run the step-2b
sweep `python3 -m src.quant_rnd.ppl research/data/gpt2.safetensors`
(7-scheme default sweep, g128, 406-token eval text, fp32 reference 53.50).
No code changes; the O(n) reconstruct fix made this feasible (~7.7 min
total for all 7 schemes).

**Results — first real perplexity numbers for the candidate family:**

| scheme | ppl | bpw |
|---|---|---|
| ternary_lloyd (full fit) | 2764.68 | 1.710 |
| ternary_outlier | 3435.21 | 2.060 |
| ternary_1step_ds | 13225.77 | 1.835 |
| ternary_1step | 70997.81 | 1.710 |
| int2_symmetric | 97457.54 | 2.125 |
| ternary_uniform | 515904.50 | 1.710 |
| dual_scale_ternary | 2408421.00 | 1.835 |

**Honest reading (negative result first):** EVERYTHING collapses vs the
fp32 53.50 reference — the best scheme is still 52x worse. Simple
group-wise quantizers at <=2.2 bpw do not produce a usable GPT-2 124M.
That is a real result, not a harness bug: the fp32 53.50 reference is
sane (forward pass verified), and the ordering structure (below) matches
theory too well to be random breakage.

**Does the SQNR ranking transfer to perplexity? — PARTIALLY.** The top
tier transfers directionally: the fitted ternary family dominates, and
fitted-vs-naive at matched bitrate is 186x (lloyd 2764 vs uniform
515904 @ 1.710 bpw). But below the fitted tier the SQNR ordering
scrambles: dual_scale_ternary SQNR-beats ternary_uniform (5.60 vs 5.55
dB) yet ppl-LOSES 4.7x (2.4M vs 515k); int2_symmetric is SQNR-worst
(2.95 dB) yet ppl-beats uniform 5.3x (97k vs 515k). Two targeted
diagnostics rule out the obvious artifacts: full-model per-group SQNR
for dual_scale_ternary shows NO pathological groups (663,552 groups,
min 0.14 dB, zero groups < 0 dB), and ternary_1step has NO dead groups
(0% all-zero, max zero-fraction 0.992). Conclusion: in collapse
territory, average-power SQNR does not predict perplexity ordering
among also-rans — a few worst-case tokens dominate the mean NLL, so
treat sub-fitted-tier ordering as cliff noise.

**Surprise that revises a prior conclusion:** full Lloyd fit beats
1-step by 25x in perplexity (2764 vs 70997) at only 0.2 dB SQNR
difference (6.93 vs 6.72). The "1-step captures 85% of the win" claim
was SQNR-true but perplexity-incomplete: in collapse territory, tiny
fidelity deltas amplify wildly through 12 layers. 1-step captures ~85%
of the SQNR win but only ~38% of the log-perplexity win over uniform.
The practical-encoder story needs the caveat: 1-step is the cheap
encoder, but its quality gap to full fit is much larger than SQNR
suggested.

**What this means for the R&D thread:** the simple group-wise fidelity
path is exhausted — not just on SQNR (Lloyd owns it) but on perplexity
(everything collapses). The honest next fidelity step is second-order
correction (OBQ/GPTQ-style), which is what makes real 2-bit methods
work; without it we are measuring the cliff face, not a usable regime.
The compute-advantage story (opcount: 1.36x decode ceiling, 3.5x energy
proxy) is untouched by this — it never depended on these schemes being
usable as pure group-wise quantizers.

**Backlog grown:** (1) int2_kmeans_q8 perplexity run — the
fidelity-gradient check (~8 min: was excluded from DEFAULT_SWEEP for
speed); if ppl recovers substantially at 9.68 dB SQNR it confirms the
harness measures a real gradient. (2) Full ternary_lloyd_ds perplexity
— missing from this sweep (only n_iter=1 1step_ds ran). (3) OBQ-style
second-order correction prototype — the real next fidelity step.
(4) Methodology caveat for future sweeps: perplexity only discriminates
below ~10x fp32; in collapse territory report ordering as directional.
(5) ternary_lloyd perplexity at g64 (SQNR-best size, ~5 min quantize).

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched (no code
changed this session — log + roadmap only, so no suite re-run needed;
tree was 127/127 green at 04:45). No secrets. No downloads.

## Engineering session 2026-09-21 ~05:45 PDT — the two missing perplexity runs (kmeans_q8 + full dual Lloyd)

**Picked:** the two top unchecked perplexity items from the 05:15 session
— the int2_kmeans_q8 fidelity-gradient check and the full
ternary_lloyd_ds (n_iter=20) run. Single sweep:
`python3 -m src.quant_rnd.ppl research/data/gpt2.safetensors --schemes
int2_kmeans_q8,ternary_lloyd_ds` (exit 0, ~9.5 min total).

| scheme | ppl | bpw | secs |
|---|---|---|---|
| int2_kmeans_q8 | **795.96** | 2.375 | 451.8 |
| ternary_lloyd_ds | **2719.46** | 1.835 | 116.0 |

(fp32 reference: 53.50. Previous best: ternary_lloyd 2764.68.)

**Result 1 — fidelity gradient check: ANSWERED YES (directionally).** The
classical 9.68 dB-SQNR reference gives 795 ppl, 3.5x better than the
previous best and the first scheme under 1000. The harness measures a
real fidelity gradient — higher SQNR does buy perplexity, monotonically
so far: 795 (9.68 dB) < 2764 (6.93 dB) < 3435 (6.77 dB) < 13225 <
70997 < 97457 < 515904 < 2.4M. Honest bound: 795 is still 14.9x the
fp32 reference, above the 10x methodology threshold — not out of
collapse territory, just less deep in it. The gradient is real but
simple group-wise Lloyd alone won't reach usable ppl at these bpws;
OBQ-style second-order correction is the next real step (already in
backlog).

**Result 2 — full dual Lloyd: ~TIED with symmetric.** ternary_lloyd_ds
at n_iter=20 gives 2719 vs ternary_lloyd's 2764, despite +0.07 dB SQNR
and 1.835 vs 1.710 bpw. The dual-scale fit buys essentially nothing
extra in perplexity at GPT-2 124M scale; symmetric ternary_lloyd stays
the ternary reference (cheaper, simpler, equal). Note the asymmetry of
the fit-depth gaps: full-vs-1step is 25x symmetric (2764 vs 70997) but
only 4.9x dual (2719 vs 13225) — the dual encoder converges closer in
one iteration, consistent with the 01:45 session's slower-convergence
finding (seed 9 needed n=5; the extra side buys less per iteration).

**Consolidated perplexity ladder (g128, 406 tokens, fp32 ref 53.50):**

| ppl | scheme | bpw |
|---|---|---|
| 53.50 | fp32 | 32 |
| 795.96 | int2_kmeans_q8 | 2.375 |
| 2719.46 | ternary_lloyd_ds (n=20) | 1.835 |
| 2764.68 | ternary_lloyd (n=20) | 1.710 |
| 3435.21 | ternary_outlier | 2.060 |
| 13225.77 | ternary_1step_ds | 1.835 |
| 70997.81 | ternary_1step | 1.710 |
| 97457.54 | int2_symmetric | 2.125 |
| 515904.50 | ternary_uniform | 1.710 |
| 2408421.00 | dual_scale_ternary | 1.835 |

**Backlog grown:** kmeans_q8 g256 (2.188 bpw) ppl run — pins the fidelity
anchor the OBQ work must beat at the opcount reference config; ablation
on whether fp32 embeddings/LN are propping up the 795.

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched (no code
changed — log + roadmap only). No secrets. No downloads.

## Engineering session 2026-09-21 ~06:15 PDT — kmeans_q8 perplexity at g256 (fidelity-anchor decision)

**Picked:** the unchecked `int2_kmeans_q8` g256 perplexity item — pins the
fidelity anchor the OBQ work must beat and decides the opcount
reference config. Single-scheme run:
`python3 -m src.quant_rnd.ppl research/data/gpt2.safetensors --schemes
int2_kmeans_q8 --group-size 256` (exit 0, 320.4 s; --group-size CLI
already existed, no code changes).

| scheme | ppl | bpw | secs |
|---|---|---|---|
| int2_kmeans_q8 (g256) | **1085.61** | 2.188 | 320.4 |
| int2_kmeans_q8 (g128, earlier) | 795.96 | 2.375 | 451.8 |

(fp32 reference: 53.50.)

**Result: g256 LOSES.** Despite only a -0.59 dB SQNR dip (9.68 -> 9.09),
perplexity degrades 37% (795 -> 1086) — the fidelity gradient prefers
the smaller group size. Per-group codebook fidelity beats the 0.19 bpw
rate saving. Consequences, both recorded in the roadmap:
(1) the OBQ anchor is **795.96 @ g128 (2.375 bpw)**; (2) the opcount
decode ratios (1.36x sym / 1.27x dual) are already computed at g128, so
no opcount re-run is needed — the reference config stands.

**Honest caveat:** single eval text (406 tokens) and one seed; robustness
re-check added to backlog before the 795 anchor is treated as gospel.

**Backlog grown:** g128-vs-g256 robustness probe; OBQ first slice
(Fisher-reweighted Lloyd centroids); ternary zero-rate-widening probe.

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched (no code
changed — log + roadmap only). Full suite 127/127 green. No secrets. No
downloads.

## Engineering session 2026-09-21 ~06:45 PDT — fidelity-anchor robustness probe + ternary g64

**Picked:** the two top unchecked perplexity items from the 06:15 session —
"robustness of the g128-vs-g256 ppl gap" (second eval text) and
"ternary_lloyd perplexity at g64". Both run-only: the ppl CLI already had
--eval-text/--group-size, so zero code changes (only .gitignore gained
`!research/data/eval_text2.txt`).

**New eval text:** `research/data/eval_text2.txt` — hand-composed non-fiction
prose (bicycle physics), 393 tokens, fp32 ppl 63.18 (sane band, ~40 s
forward). Different genre/vocabulary from eval_text.txt's literary prose.

**Result 1 — the 795.96 anchor is text-sensitive.** int2_kmeans_q8 g128 on
text2: **1047.35 @ 2.375 bpw** vs 795.96 on text1 (fp32 refs 63.18 vs 53.50).
Relative-to-fp32: 16.6x vs 14.9x. So ~+30% absolute / ~+11% relative text
sensitivity. Verdict: 795.96 is NOT gospel — the OBQ fidelity anchor must be
quoted as a text-conditioned range, and OBQ candidates re-measured on the
same text(s). Directional story holds: still the far best scheme, same
collapse band, still the gradient anchor. (The "shuffled block sample" half
of the robustness item and a g256-on-text2 check are new backlog items.)

**Result 2 — g64 buys ternary nothing in perplexity.**
ternary_lloyd --group-size 64 on eval_text: **2729.23 @ 1.835 bpw**
(229.6 s) vs 2764.68 @ 1.710 at g128. The g64 SQNR edge (7.07 vs ~6.8 dB)
translates to ~1% ppl at +0.125 bpw cost. g128 stays the reference group
size for both the ternary and classical references; no opcount re-run.

**Backlog grown:** g256-on-text2 transfer check; shuffled-block eval
protocol (mean/std over disjoint blocks) to replace the
single-contiguous-block protocol whose text sensitivity this probe
demonstrated.

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched (no code
changed). Suite 127/127 green. No secrets. No downloads.

**Mirror:** gh_push.py clean ("collected 45 files / pushed commit
9a9d31b0dc51"); GitHub main's commit message matches local HEAD.
Mirror current.

## Engineering session 2026-09-21 ~07:15 PDT — OBQ first slice (Fisher-reweighted Lloyd) + g256-text2 transfer check

**Picked:** the top unchecked R&D item — "OBQ first slice: per-group
empirical-Fisher reweighting of the Lloyd centroid fit" — plus the
remaining half of the robustness item (g256 on eval_text2), run in
parallel while coding.

**Landed:** commit `aba2394` (+ GitHub mirror 8b48688d6c6a, verified:
main's commit message matches local HEAD). Suite 141/141 green (+14).

- `src/quant_rnd/fisher.py` (new): diagonal empirical-Fisher importance
  — `GPT2.forward` gained an optional `capture` dict (activation/MLP
  intermediates keyed by tensor name; logits bit-identical with/without
  capture, tested), `d_j = mean_t(x_{t,j}^2)` per input channel,
  `per_weight_importance` expands channel energy to per-weight
  importance aligned with `.ravel()`.
- `schemes.py`: `_lloyd_1d` accepts optional per-element weights
  (weighted assignment + weighted refit); the w=None path is kept
  VERBATIM — a scratch check showed float32 `.mean()` vs float64
  weighted-mean differ in the last ulp (29/30 groups), which can flip
  boundary assignments, so the branch preserves all published anchor
  numbers bit-for-bit (pinned by test). `int2_kmeans_q8` gains
  `sample_weight` (length-checked, zero-padded tail gets zero weight).
- `ppl.py`: `quantize_model(..., sample_weights=...)` passes weights
  only to encoders accepting the kwarg (inspect-based; other schemes
  ignore); CLI `--fisher` collects weights from one fp32 forward.

**Result 1 — OBQ first slice: delta ~0 (honest negative).**
int2_kmeans_q8 g128 with Fisher reweighting: **795.93 @ 2.375 bpw** vs
795.96 unweighted (446 s). The reweighting provably changes the
codebook (synthetic test: codes+scales differ on all 10 seeds) but buys
nothing in perplexity. Per the item's pre-registered decision rule: skip
to full per-weight error-compensation (GPTQ/OBQ-style); do not bother
with a Fisher-weighted ternary variant. Caveats: diagonal Hessian only;
calibrated on the eval text itself (no held-out corpus yet — noted for
the shuffled-block protocol item).

**Result 2 — g128-beats-g256 verdict transfers to text2.**
int2_kmeans_q8 g256 on eval_text2: **1971.77 @ 2.188 bpw** (316 s) vs
g128 1047.35 @ 2.375. The gap WIDENS on text2 (88% vs 37% on text1) —
g128 wins on both texts, so the 795.96 anchor decision was not text-luck.

**Backlog grown:** full per-weight error-compensation prototype
(GPTQ/OBQ-style), sliced one-layer-first; notes that fisher.py's capture
plumbing is reusable and the X^T X inverse is the hard part on this VM
(block-wise/iterative solve suggested).

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched. No
secrets. No downloads. Mirror current.

## Engineering session 2026-09-21 ~08:05 PDT — embeddings/LN ablation (795 not limited by passthrough)

**Picked:** the top unchecked R&D item — "is the 795 ppl at 2.375 bpw
limited by embeddings/LN?" Also folded in a docs correction for the
OLLAMA_KV_CACHE_TYPE backlog item (see below).

**Landed:** commit `b9984f1` (`ppl.py --quantize-embeddings`;
`EMBEDDING_MARKERS` / `is_embedding()` mirroring realweights' exclusion
list; quantizes wte+wpe with the same scheme, biases/LN stay fp32;
`run_sweep`/`main` thread the flag; bpw bookkeeping provably unchanged
by the flag). +4 tests (flag on / default off / marker matching / CLI
parsing). Suite 145/145 green. Mirror current: GitHub main
`af269d7b817d`, commit message matches local HEAD (b9984f1).

**Result — ANSWERED NO, decisively.** int2_kmeans_q8 g128 on eval_text1
with embeddings also quantized: **ppl 1.3e14 @ 2.375 bpw** (649 s) vs
**795.96** with fp32 embeddings. Total collapse — unsurprising in
hindsight (wte is the tied lm_head, so 2-bit group-wise quantization of
the output projection destroys the logits). The fp32 passthrough is
load-bearing, NOT the bottleneck: the fidelity problem lives in the
linear weights, so the OBQ/error-compensation thread stays aimed at
linears only. Caveat: wte and wpe were quantized together — the
wte-vs-wpe split was skipped as a second ~11 min run; folded into the
embedding-table probe item, which now reads as "higher-precision or
per-row embedding schemes, not naive 2-bit" (the collapse closes the
naive version of that question).

**Backlog grown/edited:** embeddings item checked; embedding-probe item
narrowed to q8/q4 or per-row schemes + wte/wpe split. OLLAMA_KV_CACHE_TYPE
item annotated: Hearth talks to an already-running `ollama serve` over
HTTP (src/llm.py, run.sh) — there is no launcher to wire the env var
into; it belongs in the Mac's `ollama serve` environment, to be set when
Justin validates on the Mac.

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched. No
secrets. No downloads. Tree clean at session start (committed the 07:15
log entry first as d90c5c3).

## Engineering session 2026-09-21 ~08:40 PDT — OBQ one-layer slice landed (52.65 vs 53.34)

**Picked:** the top unchecked R&D item — full per-weight error-compensation
prototype (GPTQ/OBQ-style), sliced one-layer-first per the item's own plan.

**Landed:** commit `057d1b6` (`src/quant_rnd/obq.py` + `ppl.py
--one-layer/--obq/--obq-damp`; 167 tests green, incl. 17 new OBQ tests +
5 new ppl plumbing tests). Damped inverse Hessian via Cholesky (1% GPTQ
damping — load-bearing: T=406 < d_in=768 so H is rank-deficient), GPTQ
block update over input-channel rows, per-column 4-centroid Lloyd with
the int2_kmeans_q8-equivalent 8-bit codebook at matched 2.375 bpw.
Unit-verified: single-block output is bit-identical to naive per-column
quantization; compensation strictly reduces the Hessian-weighted
objective (the core math claim); block update pinned against a manual
formula recomputation; bpw formula identical to int2_kmeans_q8 by
construction. Mirror current: GitHub main commit message matches local
HEAD (057d1b6).

**Result — machinery verified end to end, directionally right.**
h.0.attn.c_attn (768x2304) on eval_text1 @ g128, 2.375 bpw:
naive one-layer 53.34 vs **OBQ one-layer 52.65** vs fp32 53.50.
Honest read: one layer is ~1.4% of params, so this probe sits near the
noise floor (naive itself barely moves ppl) — the OBQ delta is real but
small in absolute terms. The real verdict needs the full-model slice.

**Backlog grown:** OBQ full-model slice (all 48 linears, ~10-15 min on
this VM, one capture forward already covers every layer) with a
pre-registered decision rule: < ~400 ppl vs the 795.96 anchor keeps the
fidelity path alive; delta ~0 closes the second-order story on GPT-2
124M at this scale. Suggested wiring: `ppl.py --obq-all` reusing
quantize_model's target loop.

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched. No
secrets. No downloads. Suite 167/167 green before commit.

## Circuit breaker — 2026-09-21 ~10:10 PDT — full-model OBQ measurement blocked

**Verdict:** the last TWO sessions (09:15, 09:45 PDT) both failed to land
anything, so this writeup replaces another code attempt per the standing
circuit-breaker rule.

**Blocker:** the decisive full-model OBQ perplexity measurement (48 linear
layers, g128, 2.375 bpw, expected ~10-15 min runtime) cannot complete inside
the ~25-minute session execution window on this VM.
- 09:15 session: started `ppl.py --obq-all`, killed by the 1500s execution
  timeout at ~20 min elapsed, no result, no commit.
- 09:45 session: first background attempt killed by a service restart
  (/tmp wiped, no output); second attempt wrote to durable storage but was
  again cut off by the execution timeout at ~14 min, no result, no commit.

**What is NOT blocked:** the machinery itself. `quantize_layers_obq` and
`ppl.py --obq-all` are committed (7cb610e), 179 tests green, one-layer
slice measured 52.65 vs naive 53.34 vs fp32 53.50 (directional, near noise
floor). The missing piece is only the completed full-model run.

**Unblock options for a later session (no code changes made now):**
1. Chunked measurement: run OBQ per block-group across sessions, saving
   intermediate quantized weights to research/data/ (survives restarts).
2. Detached runner: launch the full run as a `nohup` process writing to
   durable storage; sessions poll instead of blocking.
3. Reduce eval tokens for the measurement run only (pre-registered, noted
   as a methodology deviation in the log).

**Next step:** user notified; resume on one of the above once acknowledged.

## Housekeeping — 2026-09-21 ~10:15 PDT — circuit breaker held

- Session opened to a dirty tree (the 10:10 circuit-breaker log entry
  uncommitted + a stale `research/log/tmp_obq_all_run.out` partial from
  the killed 09:45 run). Per rule 1/7: NO new code attempts; no re-run
  of the full-model OBQ measurement.
- Cleaned: removed the stale tmp output; committing the pending
  circuit-breaker log entry so the tree is clean for the next session.
- Rule 12: recorded the three unblock options as a new unchecked
  backlog item in ROADMAP_70B.md ("Quant R&D: unblock the full-model
  OBQ measurement") — checkpoint-per-block / detached-runner /
  fewer-eval-tokens. No implementation started.
- Blocker status unchanged: awaiting Justin's acknowledgment / direction
  on which unblocker to build.

## Survey — 2026-09-21 ~10:50 PDT — avenue C (GGUF sub-2-bit 70B) surveyed, effectively closed

Per rule 7 the blocked full-model OBQ measurement was NOT re-attempted
(and per the 10:15 note the unblocker choice stays with Justin); this
session worked the two oldest unchecked avenue-C backlog items instead.

**Ollama TQ1_0/TQ2_0 support: YES (verified).** Ollama main and release
v0.34.3-rc1 both pin llama.cpp b10969 (LLAMA_CPP_VERSION); at that
revision upstream ggml/include/ggml.h defines GGML_TYPE_TQ1_0 and
GGML_TYPE_TQ2_0 (raw-fetch verified). Ollama loads TQ GGUFs. Caveat:
type-level support only — Metal kernel/perf path for ternary matmuls
unverified, needs the Mac.

**HF survey: no 70B-scale TQ1_0/TQ2_0 GGUF exists.** HF API text search
returns only unrelated small repos + one 397B oddity. Nothing to evaluate.

**70B IQ1_M exists but fails the budget on two counts.** bartowski's
Llama-3.1-70B, Llama-3.3-70B, r1-1776-distill-llama-70b IQ1_M files all
measure 16.75 GB (HF API tree) — above the roadmap's ~15.3 GB estimate
and above the 10–11 GB usable budget even before macOS + KV cache. Cannot
fit a 16 GB Mac, full stop. Quality would not save it: bartowski
"Extremely low quality, *not* recommended."; mradermacher i1-IQ1_M (16.0
GB) "mostly desperate"; independent 20-model Llama-3-70B test put IQ1_M
at 12–15/18 with degraded instruction-following ("1-bit quantization
doesn't seem viable yet" for agentic use).

**Net:** avenue C's dense-70B option (a) doesn't exist as a file (TQ),
(b) doesn't fit when it does (IQ1_M), (c) loses too much quality for an
agent. Reclassified WATCH-only in the roadmap (for future ≤1.3-bpw GGUF
quants or TQ files on smaller dense models — e.g. ~50B TQ1_0 ≈ 10.6 GB
would fit the budget).

**Landed:** commit d6b2971 (roadmap only; suite 179/179 green before
commit), mirrored to VihaanGopee/hearth@main — message verified to match.
**Backlog grown:** two new items (re-survey HF when ternary ecosystem
matures, size-check-first rule; Mac-side Metal kernel-path verification).

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched. No
secrets. No downloads (API metadata only). Rule 12 satisfied via the two
new backlog items.

## Quant R&D — 2026-09-21 ~11:35 PDT — multi-text eval protocol landed; ternary tiering is text-robust

**Feature (commit 9582af2, mirrored, message verified):** `ppl.py --eval-texts A.txt,B.txt`
evaluates every scheme on every text — quantize once per scheme, forward per text —
and prints a per-text + mean/std + x_fp32 report. `fp32` is a pseudo-scheme for a
same-table unquantized reference (bpw 32.0). Single-text behavior is byte-identical
to before; OBQ probes stay on the first text (noted on stdout); `--fisher` captures
on the first text only (documented). 5 new tests, suite 184/184 green before commit.
Serves the "shuffled-block eval sample" item's multi-text half (roadmap updated:
item now PARTIALLY LANDED; shuffled-block-from-longer-corpus half still open).

**Measurement (first real --eval-texts run, 3-scheme ternary tiering slice, g128):**

| scheme | bpw | text1 | text2 | mean | std | x_fp32 |
|---|---|---|---|---|---|---|
| fp32 | 32.000 | 53.50 | 63.18 | 58.34 | 4.84 | 1.00 |
| ternary_lloyd | 1.710 | 2764.68 | 3812.24 | 3288.46 | 523.78 | 56.37 |
| ternary_1step | 1.710 | 70997.81 | 151958.28 | 111478.05 | 40480.23 | 1910.87 |
| ternary_uniform | 1.710 | 515904.50 | 907972.44 | 711938.47 | 196033.97 | 12203.49 |

**Verdict:** the step-2b fitted-vs-naive tiering is TEXT-ROBUST — lloyd >> 1step >>
uniform at matched 1.710 bpw on both texts, and the fitted-vs-1step gap widens on
text2 (~40x vs ~25x on text1). Per the methodology item, everything is far above
the 10x-fp32 discrimination line (lloyd mean 56x), so the numbers are
directional-only — but the ORDERING is not text-luck. Naive schemes are more
text-sensitive in relative terms (uniform 1.76x across texts vs lloyd 1.38x).
Path validation: text1 values reproduce the logged step-2b numbers bit-identically
(53.50/63.18/70997.81/2764.68) — the refactor changed no numerics.

**Ops notes:** the planned int2_kmeans_q8 g128-vs-g256 × 2-text anchor run was
started in the background but killed by a service restart (same failure mode as
the 09:45 session); the ternary slice was then re-run in the FOREGROUND and
completed (~8 min). The anchor re-measurement is still open — next session can
run it directly with the new harness:
`python3 -m src.quant_rnd.ppl research/data/gpt2.safetensors --schemes fp32,int2_kmeans_q8 --group-size 128 --eval-texts research/data/eval_text.txt,research/data/eval_text2.txt`
(then again with --group-size 256). OBQ full-model measurement remains blocked
awaiting Justin's unblocker choice. Default `backend: ollama` + `qwen3:8b`
untouched. No secrets. No downloads.

**Backlog grown (rule 12):** stale "adopt ternary_lloyd as reference" item marked
superseded (ternary_1step + g128 anchor are the adopted references); shuffled-block
item updated to PARTIALLY LANDED; ternary-sweep-on-text2 item PARTIALLY ANSWERED
by this slice (remaining schemes low value).

## Quant R&D — 2026-09-21 ~12:05 PDT — OBQ session-timeout unblocker landed; first measurement chunk running

**Feature (commit 6fa4635, mirrored, message verified):** `src/quant_rnd/obq_ckpt.py`
+ `ppl.py --obq-all --obq-ckpt-dir DIR [--obq-max-layers N]` — the standing-direction
authorized unblocker for the blocked full-model OBQ measurement. Each layer's
quantized weights are checkpointed to research/data/obq_ckpt/ (gitignored) as it
finishes; later runs read the manifest, skip done layers, and resume from the
printed resume point. Manifest records group_size/damp_frac/n_iter/calibration-text
hash and refuses to mix incompatible params; resumed assembly is bit-identical to
an in-memory --obq-all (test-pinned; quantization is deterministic). 18 new
tests, suite 202/202 green before commit. One bug caught by the tests during
development: np.savez appends .npz to non-.npz temp names, breaking the atomic
rename — fixed by temp-naming with the .npz suffix.

**Measurement:** first checkpointed chunk COMPLETED this session — 12/48 layers in
391 s (~33 s/layer; the per-column Lloyd fit dominates), exit 0. Manifest at
research/data/obq_ckpt/manifest.json; resume point: **h.11.attn.c_attn.weight**
(36 layers remain ≈ 20 min — size next chunk's --obq-max-layers to the session
budget, e.g. 24–30, and finish with a final small chunk that also runs the
assembly + perplexity verdict). Resume command:
`python3 -m src.quant_rnd.ppl research/data/gpt2.safetensors --obq-all
--obq-ckpt-dir research/data/obq_ckpt --obq-max-layers 24`
Note: layers process in plain string-sorted order, so h.10/h.11 sort before h.2
— deterministic and harmless; the manifest tracks by name.

**Backlog grown (rule 12):** the "unblock the full-model OBQ measurement" item is
now PARTIALLY LANDED (machinery built + first chunk running); the OBQ full-model
slice item now carries the chunked protocol and resume command so any session can
continue. **Resume protocol:** re-run the same command; status line prints
done/48 + next layer name; on the final chunk the run assembles the model and
prints the verdict vs 795.96 with the <~400 decision rule.

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched. No secrets. No
downloads. Roadmap-only changes committed separately below.

## Quant R&D — 2026-09-21 ~12:15 PDT — OBQ full-model verdict IN (475.48); fidelity ladder exhausted; attention term landed

**OBQ full-model verdict (priority #1, complete):** the checkpointed
protocol worked exactly as designed — chunk 2 (24 layers, 770 s) then the
final chunk (12 layers, 428.7 s incl. assembly + perplexity) ran this
session; manifest 48/48 COMPLETE, bit-identical assembly, survived a
background restart across three sessions. **Full-model OBQ ppl: 475.48 @
2.375 bpw g128** vs naive anchor 795.96 (eval_text1, 406 tokens, calibrated
on the eval text itself). That is 1.67x better than naive — but 8.9x above
the fp32 reference (53.50), still in collapse territory per the harness's
own decision rule, and the <~400 "fidelity path alive" bar was NOT met.
Middle ground, logged honestly: the delta is real (OBQ > Fisher
reweighting > Lloyd > naive on the ladder) but insufficient.

**Conclusion per the item's decision rule: the pure group-wise fidelity
ladder is EXHAUSTED at this scale.** naive -> Lloyd k-means ->
Fisher-reweight -> full per-weight OBQ at ~2.4 bpw all collapsed on
GPT-2 124M. No further small-scale fidelity iteration at this bitrate is
expected to change that; the quant-R&D story now rests on the compute side
(ternary add/sub kernels; opcount ceilings 1.36x decode / 1.79x prefill)
and on fundamentally different foundations (native-ternary models, MoE,
Mac-side validation). New backlog item: a fidelity-retrospective with the
one-experiment-max rule for structurally different ideas
(sensitivity-adaptive bit allocation at matched average bpw).

**Feature (commits cd60e81 + roadmap 2e17003, mirrored, messages verified):**
attention O(L^2) term in `prefill_roofline_tps` (backlog item closed):
4*n_layers*n_q_heads*L^2*head_dim FLOPs (MAC=2) + KV read = KV write,
`include_attention=False` recovers the matmul-only model. 203 tests green
(new `test_prefill_attention_term_math` pins formula, KV-read bytes, L^2
scaling, GQA scaling, validation). Honest consequence at 32k+: attention
DOMINATES the matmuls, so the ternary-vs-kmeans prefill ceiling ratio
compresses toward 1 — the prefill compute case largely evaporates at very
long context, while decode keeps the 1.36x ratio. New backlog item: pin
the crossover context length where the ratio drops below ~1.2x.

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched (suite
covers it). No secrets. No downloads. OBQ checkpoint data stays in
research/data/obq_ckpt/ (gitignored) for reuse; the 475.48 verdict is
text1-conditioned (calibrated on the eval text — no held-out corpus on
this VM, as previously noted).

**Next:** breadth per the standing direction — MLX backend prototype,
MoE survey, model cascade, speculative decoding, KV-cache Mac note.

## Quant R&D — 2026-09-21 ~12:45 PDT — prefill crossover length PINNED (64k)

**Feature (commit 1980f01, mirrored, message verified):** `prefill_crossover_L()`
in `src/quant_rnd/opcount.py` closes the crossover backlog item: it sweeps
prefill ceilings over prompt lengths and pins where scheme A's advantage
compresses below a threshold as the scheme-independent attention O(L^2)
term swamps the matmul difference. For the reference pair (ternary_1step
1.710 bpw vs int2_kmeans_q8 2.375 bpw, 70B scale / 200 GB/s / 5.2 TFLOPS
M1 Pro GPU peak): ratios 4k=1.63x, 8k=1.52x, 16k=1.39x, 32k=1.26x,
64k=1.15x, 128k=1.09x. The ratio drops below 1.2x between 32k and 64k
(crossover_L=65536); below 1.5x at 16384. 205 tests green (two new tests
pin the ratio table, edge cases, and input validation; one initial test
draft had wrong monotonicity expectations — fixed and re-run green).

**Practical read:** the ternary prefill compute case survives at ordinary
prompt lengths (<=32k) but compresses to noise at 64k+ context. Decode
(bandwidth-bound, no attention term) keeps the 1.36x ceiling ratio at ALL
context lengths — the decode case is the durable half of the opcount
story. Numbers are model outputs for 70B-shaped Llama geometry (GQA-8,
head_dim 128); re-pin if the target architecture changes.

**Backlog grown (rule 12):** new item — Mac-side validation of the
crossover (measure prefill tok/s for matched-size ternary/TQ1_0 vs 2-bit
codebook models at 4k-64k prompts on Justin's Mac; extends the existing
prefill-validation items).

**Ops notes:** default `backend: ollama` + `qwen3:8b` untouched. No secrets.
No downloads.

## Engineering — 2026-09-21 ~13:15 PDT — speculative decoding in llamacpp backend

**Feature (commit b7cd64c, mirrored, message verified):** `LlamaCppClient` gains
`speculative="off"|"prompt_lookup"|"draft_model"`, `draft_model_path`,
`draft_n_tokens` (upstream default 10). prompt_lookup uses the verified
public API (`LlamaPromptLookupDecoding(num_pred_tokens=...)` wired through
`Llama(draft_model=...)`); draft_model mode loads a small draft GGUF via
`LlamaDraftModel` — constructor signature NOT verifiable on this VM (package
absent), so the branch is marked untested-on-Mac and translates TypeError
into a clear LLMError. Mode/path validation happens up front with clear
errors. Default "off": existing behavior byte-for-byte unchanged (suite
pins it). Config keys `llamacpp.speculative` / `draft_model_path` /
`draft_n_tokens` wired in agent.py as keyword args.

**Tests:** 12 new tests in tests/test_speculative.py (fake llama_cpp module
injection: mode wiring, num_pred_tokens pass-through, path validation,
unknown-mode rejection, chat/tools behavior unchanged, agent config wiring).
Full suite 217 green (1 skipped: pre-existing), after fixing a ResourceWarning
in the new test's temp-file writes. Self-reviewed diff; default
`backend: ollama` + `qwen3:8b` untouched. No secrets. No downloads.

**Why it matters for the mission:** decode is bandwidth-bound, so a good
draft can multiply decode tok/s on the 70B path — orthogonal to the quant
ceiling ratios pinned earlier (1.36x ternary/kmeans decode ceiling).
Follow-up backlog items added: Mac-side measurement of prompt_lookup
(num_pred_tokens=10 vs 2) and draft-model modes vs non-speculative baseline
at matched quant, to quantify realized speedup vs the opcount ceilings.

**Next:** model cascade, MLX backend prototype, low-active-parameter MoE
survey, KV-cache Mac note, benchmark harness.

## Quant R&D — 2026-09-21 ~14:15 PDT — low-active-parameter MoE survey LANDED

**Survey (research/moe_survey_2026-09-21.md):** answered the end-of-week
usable-solution question with numbers. Central candidate: Qwen3.5-35B-A3B
(35B total / 3.3B active, 256 experts 8+1 shared, 40 layers, 262k ctx).
Budget math: 34.65 B params, so GB ≈ 34.65 × bpw / 8.
- In-budget in-RAM TODAY: unsloth IQ2_M GGUF (10.6 GB, ~2.45 bpw). Quality
  at that bitrate is UNVERIFIED — the week's single most important unknown.
- Borderline: mtrpires mixed-IQK (11.38 GB disk / ~12.5 GB RAM; attention +
  shared expert kept high-precision, routed experts aggressive — principled
  recipe, no independent benches) and unsloth Qwen3.6-35B-A3B-MTP IQ2_XXS
  (11.819 GB, native MTP head — composes with this morning's speculative
  backend).
- Best benchmarked ~2-bit: oQ2 MLX (oMLX docs: MMLU 64.0% vs naive 2-bit
  14.0%; TruthfulQA 80.0%; HumanEval 78.0%; MBPP 63.3%) — but ~12.6 GB,
  over the usable budget for in-RAM; flash-paging only. oQ3 (~15.2 GB)
  hits MMLU 85.0% ≈ FP16. Jundot publishes prebuilt oQ models on HF.
- Excluded: Qwen3-30B-A3B 4-bit MLX (~17 GB), EOQ Q5 (35.2 GB, PPL +0.2),
  MINT (37 GB min), Q8_0 GGUF (36.9 GB), Q4_K_M 20–22 GB (flash-streaming
  ~1.5 tok/s — not usable).

**Honest verdict:** no option is 70B-dense quality (oQ2 64% MMLU vs FP16
~85%). "70B-class" must be measured against dense models that fit 16 GB,
not assumed. Recommended Mac validation order in the note: IQ2_M first
(tok/s + MMLU subset on the M1 Pro), then mixed-IQK, then self-quantized
oQ2 via oMLX's 16 GB-friendly streaming path.

**Backlog grown (rule 12):** checked survey item + 4 new follow-ups (Mac
tok/s+quality for IQ2_M and mixed-IQK, Hearth llamacpp recipe/profile for
the 35B-A3B, HF watch for prebuilt oQ2 35B-A3B). JANG tracking stays open
(no new 2-bit MoE reports found today).

**Ops notes:** docs-only commit; full suite run green before commit. Default
`backend: ollama` + `qwen3:8b` untouched. No secrets. No downloads.

**Next:** model cascade, MLX backend prototype, benchmark harness,
ternary zero-rate sparsity probe, encoder vectorization.
